[package]
name = "libid-oidc-core"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Off-chain helpers for the OIDC → ZK → on-chain identity pipeline (JWT parsing, JWKS modulus → 18×120-bit limb math, packed-bytes encoding for the jwt_email circuit's public inputs)."

[dependencies]
anyhow = { workspace = true }
base64 = { workspace = true }
hex = { workspace = true }
num-bigint = { workspace = true }
num-traits = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
sha2 = { workspace = true }
thiserror = { workspace = true }

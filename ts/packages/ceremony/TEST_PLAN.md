# `@libid/ceremony` test plan

This plan tests the `@libid/ceremony` package architecture in
[ARCHITECTURE.md](ARCHITECTURE.md) and its browser protocol in
[CCDP.md](CCDP.md), its prover subsystem in [PROVER.md](PROVER.md), and the
integrating-server contract in [SERVER.md](SERVER.md).
Normative proof and platform behavior come from the
[common ceremony rules](../../../specs/ceremony-common.md) and
[identity-platform ceremonies](../../../specs/platform-ceremonies.md).

Every row is one stable requirement and may require multiple positive and
negative cases. Tests **must cite its ID**. Existing IDs retain their names;
never renumber or reuse one.

A row belongs here only when it exercises `@libid/ceremony`, an emitted browser
asset, or a server/browser contract declared by the package. Consumer-owned Job
transitions, wallet preparation or confirmation, transaction submission, and
post-ceremony recovery belong to their respective consumer test plans.

## Package and asset isolation

| ID | Assertion |
|---|---|
| LIBID-MOD-002 | The `@libid/ceremony/prover` entrypoint emits one dual-context `libid-ceremony-prover.js`: its Window branch owns fixed native-DOM prover UI, coordinator/window relay, selected-profile prefetch, and proving, while its ServiceWorker branch owns only ordinary immutable-asset Cache Storage single flights and bb.js CRS-loader single flights into bb.js's IndexedDB cache. Its bundle contains no final-proof verifier, redirect, React/UI framework, wallet key, connector, Registry submission, or wallet-policy code. |
| LIBID-MOD-003 | The `@libid/ceremony/popup` entrypoint emits `libid-ceremony-popup.js` with fixed native-DOM ceremony/progress UI; its bundle contains no proving implementation, React/UI framework, wallet key, connector, Registry submission, or wallet-policy code. |
| LIBID-MOD-011 | `@libid/ceremony/client` owns the one exact `CeremonyConfig` schema and validator, including a nonempty duplicate-free `ceremonyVersions` list for each configured platform. The application-scoped ceremony client fetches and validates it once, with no separate configuration module or application-supplied integration fields; popup and prover do not fetch it. |
| LIBID-MOD-012 | The shared launch popup loads `/api/v1/ceremony/prover`, and the prover artifact owns both selected-profile prefetch and proof execution. Prefetch exposes only `ProverPrefetchingAssets` to the application; no client prefetch API, separate prefetch module, artifact, route, mode, or separately versioned package exists. |
| LIBID-MOD-013 | `@libid/ceremony` contains no server handler or secret. `platforms/github` implements only the normative bounded `TokenRequest` and `TokenResponse` codecs and validation for the server-provided token endpoint; ccdp, client, popup, prover, worker, platform, and browser exports build with server implementation unavailable. |
| LIBID-MOD-014 | The ceremony client's public operation input contains exactly a 32-byte `chainId` hash, enabled `platformId`, 32-byte `operationDomain` hash, and bounded opaque `transactionData`. It rejects unknown fields, malformed hash lengths, ceremony version, authorization nonce, authorization digest, OAuth client, redirect, proof-domain, Registry, or finalizer values. |
| LIBID-MOD-015 | For every `CeremonyClient.new`, the client exact-validates and copies the supplied chain and operation-domain hashes without deriving them, selects its newest locally preferred `PlatformCeremonyVersion` also advertised by that platform's `CeremonyConfig`, draws a fresh cryptographically secure 32-byte authorization nonce, reproduces the normative authorization digest and code verifier, and freezes every input and derived value before provider navigation; later caller mutation changes nothing. |
| LIBID-MOD-016 | `@libid/ceremony/ccdp` builds as a pure leaf with platform catalog, authorization, browser popup, prover, platform OAuth/proving implementation, proof verifier, network, storage, Job, wallet, connector, and UI code unavailable. Client, popup, prover, and `platforms/index` consume the same codecs and reject directionally invalid messages. `ProverNotifyEvent` contains exactly its type, ceremony ID, one bounded opaque platform step, and prover-stamped numeric timestamp; it has no common stage. The client alone constructs `CeremonyEvent` with exactly a stage, optional platform step, and numeric timestamp, preserving a validated prover timestamp for a platform event and stamping a local stage event itself. It contains no type discriminator, result, counter, error code, identifier, or broader Job stage. Stages are only authorization, oauth-validation, and proof-generation; platform-step status is only started, completed, or failed. |
| LIBID-MOD-017 | One application-scoped `CeremonyClient` fetches and validates `CeremonyConfig`, excludes configured platforms missing from its closed implementation or sharing no ceremony version with it, and creates independent stateful ceremonies through `new(ceremonyId, input)`. Calling `new` for an excluded platform fails before launch. No package-global singleton, generic Host, mutable popup setter, Job-store dependency, Registry callback, or composition plugin exists. |
| LIBID-MOD-018 | A created `Ceremony` exposes only its fixed `CeremonyNavigation`, event subscription, `proveUserIdentity`, and best-effort `cancel`; authorization is not exposed as a separate instance property. `CeremonyNavigation` contains only the shared `/api/v1/ceremony/popup` `href` with a closed ceremony-ID/platform-ID/ceremony-version fragment and target. The caller owns synchronous creation of an `about:blank` popup and real-anchor fallback. `proveUserIdentity({ expectedPopup })` navigates and pins one non-null source, while omitted `expectedPopup` opens and navigates nothing and binds the matching `ProverPrefetchingAssets` source. No public provider URL, asset URL, nullable popup value, or mutable popup setter exists. |
| LIBID-MOD-019 | `platforms/index` has one closed catalog of compiled platform/version modules. Static `PlatformId`, immutable `supportedPlatforms`, and `SupportedCeremonyVersion<P>` derive from its keys, while `ProofByPlatformVersion[P][V]` derives from each version module's `validateProof` return type. Its `validateProofMessage(P, V, ProverDeliverProof<unknown>)` invokes that exact validator and returns `ProverDeliverProof<ProofByPlatformVersion[P][V]>`; any TypeScript assertion required by generic indexed dispatch is confined behind the successful runtime validator, and the Ceremony Client contains no proof cast. The package root and client API re-export those public types; ccdp does not import the aggregator. Adding, removing, or renaming a platform or version key updates the derived types and requires that version module's own validator and proof type at compile time. `OAuthProof` carries `platformCeremonyVersion` as the proof discriminator. A separately maintained platform union, version map, proof map, proof-version axis, mutation, duplication, unknown value, and dynamic registration are unavailable. |
| LIBID-MOD-020 | Each `CeremonyClient.enabledPlatforms` is an immutable intersection of `supportedPlatforms` and validated `CeremonyConfig` keys having at least one mutually supported ceremony version, in catalog order. It exposes no OAuth client, ceremony version, server configuration, or display metadata, and mutating a returned value cannot change ceremony admission. |
| LIBID-ASSET-002 | After URL clearing, redirect and prover bootstraps dynamically append their exact root `modulepreload` with immutable URL, SRI, and anonymous CORS, await success, and import that identical URL from the module map; changing bytes, URL, integrity, origin, media type, or COEP compatibility fails before execution. |
| LIBID-ASSET-003 | The prover build pins its Noir and Aztec-distributed bb.js JavaScript, workers, WASM, and CRS locations; whether JavaScript is one root file or immutable companion chunks does not change that authority. `/api/v1/ceremony/prover` embeds only the global libID-built notarization-client URL, per-profile libID circuit URLs, and the deployment-fixed Notary Service URL required by each browser-notarized profile; platform-version code pins the expected artifact digests. Ceremony, opener, query, callback, message, and application inputs cannot replace a dependency, artifact identity, or Notary Service endpoint. |
| LIBID-ASSET-004 | A `PlatformCeremonyVersion` owns one platform's authorization-digest construction, OAuth grammar, proof construction, proof type and validator, and final `OAuthProof` assembly; the ceremony client selects its newest locally preferred version advertised by the platform configuration and freezes it in the live Ceremony, `AppRequestProof`, and accepted `OAuthProof`. `platforms/authorization` supplies shared digest and PKCE helpers but has no independent version or mandatory policy: each platform-version module owns whether and how it uses them. Chain, Registry, and contract-verifier versions do not affect that selection, and distinct Consumers may accept the same output. There is no independent proof-version axis. |
| LIBID-ASSET-005 | Changing one platform ceremony version does not version another platform or `CCDPVersion`; application input cannot select it, and `CeremonyConfig` only constrains selection to its advertised set. |
| LIBID-ASSET-006 | Every live redirect/prover page retains the modules it loaded; the popup dispatches the ceremony's exact ceremony version and never resolves `latest` during a ceremony. |
| LIBID-ASSET-007 | Every live `Ceremony` and `AppRequestProof` pin the exact public `PlatformCeremonyVersion`; the popup retains every version it still accepts during its compatibility window and never resolves `latest` after creation. An unsupported version cannot be consumed and requires a fresh ceremony. |
| LIBID-ASSET-008 | One deployment platform configuration generates both `CeremonyConfig` and `ProverAssets`; they are not independently maintained platform lists. `/api/v1/ceremony/prover` embeds one exact `ProverAssets` value with one canonical immutable `notarizationClientUrl`, one canonical immutable `circuitUrl` for every enabled platform/version, and exactly one canonical `notaryServiceUrl` for each profile whose closed implementation notarizes in the browser. A non-notarized profile omits that field. Profiles are duplicate-free and match `CeremonyConfig` plus the package's single local catalog; shared artifact URLs reuse one cache entry. Missing, extra, malformed, conflicting, non-HTTPS, or digest-mismatched values fail before fetching or OAuth, except for explicit loopback development. Artifact digests, Aztec toolchain assets, and SRS size remain closed code constants; none enters either wire record. Changing a Notary Service or libID asset host without changing admitted artifact bytes is deployment-only. |
| LIBID-ASSET-009 | A real external worker graph passes with the outer blob bridge and every nested bb.js/TLSNotary worker; direct cross-origin `new Worker(url)` is rejected and no unisolated fallback runs. |
| LIBID-ASSET-010 | Rotate `CeremonyConfig` while a ceremony is live: the browser continues with the exact client ID, redirect URI, and ceremony version frozen at creation; a server exchange which no longer supports that flight fails closed, and a fresh `CeremonyClient` uses current configuration. Popup and prover perform no mid-ceremony configuration fetch. |
| LIBID-ASSET-011 | Every profile resolves the same code-pinned ACVM, Noir ABI, bb.js prover assets, and launch-wide 2^18-point BN254 SRS plus fixed Grumpkin data. X and GitHub additionally select the same configured notarization client and `bearer-link` circuit; Google selects its configured `oidc_google` circuit. The first platform starts the fixed SRS flight set, and every later platform reuses it without a size upgrade or refetch. Only missing profile-specific assets are fetched. |
| LIBID-ASSET-012 | A cleared `/api/v1/ceremony/prover#prefetch(ceremonyId, platformId, ceremonyVersion)` bootstrap causes the Window branch and service worker to fetch only the exact selected ordinary-artifact profile and start the fixed launch bb.js CRS loaders. Importing bb.js alone fetches no CRS. The fragment cannot supply an asset URL or SRS size. Profiles for other enabled platforms remain unfetched; a later `AppRequestProof` selects the same profile and cannot widen it. |
| LIBID-ASSET-013 | Circuit-release conformance records the pinned EVM circuit sizes and actual SRS floors: `bearer_link` 42,006 fails at its mathematical 2^16 ceiling because bb.js 5.2 requires a positive multiple of its 4 MiB compressed-input chunk and succeeds at 2^17; `oidc_google` 179,443 succeeds at 2^18 and fails at 2^17. The checked-in launch-wide SRS constant is 2^18 and every profile proves under it. Browser user agent and deployment data cannot alter it. A changed circuit or bb.js requirement exceeding that constant fails the build. Splitting the fetch size later requires performance evidence, not a platform ceremony version change while proof semantics remain identical. |

## Platform proving

| ID | Assertion |
|---|---|
| LIBID-PROVER-001 | Every enabled profile maps in closed code to one circuit release artifact and compatible Noir/ACVM and bb.js versions, while the package supplies the same 2^18-point BN254 SRS and fixed Grumpkin data to all of them. A real browser UltraHonk proof for every profile generated with that SRS verifies under the release's key. Changing circuit bytes, ABI, public-input order, proof system, or bb.js compatibility without the required platform ceremony version update fails; changing the shared SRS fetch policy alone does not require one while proof semantics remain identical. No application, popup message, user agent, request value, or Consumer contract selects any of them. |
| LIBID-PROVER-002 | Google ceremony version `1` emits the closed readiness, Google, and proof-engine diagnostic spans defined in PROVER.md, strictly parses the captured ID Token and selected JWK, and builds the `oidc_google` private witness. The prover delivers `GoogleProofV1` with one bounded Honk proof, the exact signed audience, subject, email and expiry, and the selected JWK modulus, but no attestation or flattened public-input array. The Ceremony Client shape-validates those values, exact-matches the audience to its retained client identifier, and derives the non-authoritative preview without importing a proof verifier. The pure Google adapter deterministically hashes and packs the Authorization Digest plus those semantic fields into exactly 56 canonical 32-byte verifier inputs; wrong field order, packing, length, or one-byte mutations fail the shared cross-language vector. |
| LIBID-PROVER-003 | X ceremony version `1` emits the closed readiness, detailed notary worker/session, attestation, circuit-input, and proof-engine diagnostic spans defined in PROVER.md, including overlapping token and identity sessions whose identity provider request waits for the token bearer. It performs the fixed token and identity TLSNotary Proxy sessions, rejects a bearer that is empty, non-printable, or longer than 128 bytes, commits the same bearer under independent 16-byte blinders, reveals only the normative request/delimiter/identity ranges, and builds `bearer-link`. The version module defines and delivers its own `XProofV1` with one bounded Honk proof plus the byte-exact signed token and identity attestations in that order; it contains no extracted-field sidecar or circuit-public-input field. The prover discards bb.js's 64-field internal array, and the two 32-byte commitment inputs reconstructed from those attestations verify the delivered proof. |
| LIBID-PROVER-004 | GitHub ceremony version `1` emits the closed readiness, token-exchange, browser-notary, circuit-input, and proof-engine diagnostic spans defined in PROVER.md. Before starting `/user`, it exact-validates the bounded token response, verifies the token attestation's original bytes and signature against the pinned notary key, requires the pinned format/platform/token-session/authority/method/path/layout, exact-matches the revealed code/client/redirect/verifier to the live Ceremony, and proves that the returned access token plus exact 16-byte `bearerBlinder` opens the uniquely framed attested commitment. Every failure discards the response and starts no identity session. The synchronous token endpoint emits no server-internal progress. The browser then runs the fixed `/user` notarization and shared `bearer-link` circuit and delivers `GitHubProofV1` with only one bounded Honk proof plus the byte-exact token and identity attestations in that order. The two commitments reconstructed from those attestations verify the proof; no extracted-field sidecar, circuit-public-input field, GitHub-only circuit, or prover backend exists. |
| LIBID-PROVER-005 | The X and GitHub profiles point to the same immutable `bearer-link` circuit URL and resolve the same global notarization-client URL and code-pinned prover toolchain. Cross-platform prefetch reuses those cache entries; platform-specific OAuth, server exchange, transcript construction, parsing, and progress remain in their respective closed modules and cannot alter the circuit ABI. |
| LIBID-PROVER-006 | The selected notary release supplies its pinned JavaScript wrapper, WASM, and worker bootstrap built from the notary repository's pinned TLSN revision, including `set_progress_callback`. X and GitHub use that exact bundle; Google neither fetches nor initializes it. Missing or mixed release members fail before a notarized request. |
| LIBID-PROVER-007 | Every successful run emits one `started` and one `completed` for every diagnostic span, including cache hits. Every span started before a failure emits exactly one terminal event, while later spans are absent. Parent spans contain their children, documented dependencies hold, and otherwise-overlapping spans need not form a total order. X exercises concurrent token and identity setup and proves that `identity-provider-request` starts only after the bearer is available. Prover timestamps permit per-span duration diagnostics without exposing URLs, credentials, witnesses, proofs, or raw errors. |
| LIBID-PROVER-008 | The notarization-client wrapper accepts only the three closed browser inputs: X token, X identity, and GitHub identity at ceremony version `1`. Each operation uses its code-owned target authority, URL, method, header order, body grammar, bounds, session tag, Proxy mode, SHA-256 commitment algorithm, and reveal/commit layout. X token returns only its bounded access token, exact 16-byte bearer blinder, and byte-exact attestation; either identity operation returns only its exact 16-byte bearer blinder and byte-exact attestation. No caller-selected target/request/layout, session ID, transcript, range list, response body, TLSNotary object, or duplicate commitment escapes. |
| LIBID-PROVER-009 | Before the notarization wrapper returns, `SHA256(accessToken || bearerBlinder)` equals the uniquely framed bearer commitment in the returned original attested-data bytes and the duplicate hash returned by raw TLSNotary. A mismatched opening, malformed layout or attestation frame, truncated or trailing bytes, failed same-channel retrieval, cancellation, or partial result rejects. Independent X token and identity blinders differ. |

## Ceremony client and Ceremony Cross-Document Protocol (CCDP)

| ID | Assertion |
|---|---|
| LIBID-OAUTH-001 | The application-scoped client fetches and exact-validates server-owned `CeremonyConfig` once; unknown fields, missing or wrongly typed values, noncanonical, empty, or out-of-range client, redirect, platform, or ceremony-version values, and duplicate ceremony-version entries fail before navigation. `allowedAppOrigins` is rejected as an unknown public field. An unknown but valid ceremony version is ignored when another advertised version is locally supported; a platform with no common version is unavailable. Popup and prover do not fetch configuration. |
| LIBID-OAUTH-002 | The frontend configuration request requires an exact browser `Origin` and matching noncredentialed CORS response. The initial redirect sends `ProverPrefetchingAssets(ccdpVersion, ceremonyId, platformId, ceremonyVersion)` without authenticating its opener. After OAuth, the new redirect sends `PopupRequestAuthentication(ccdpVersion)` without the ID; the client accepts it only from the retained popup source and configured redirect origin, then sends `AppAuthenticateOrigin(ceremonyId)`. The popup accepts that message only from its exact opener and a server-allowed browser-stamped app origin and exact-matches the ID to OAuth state before releasing `PopupDeliverParams`. No payload origin, redirect-request `Origin`/`Referer`, server session, or server action record authenticates the channel. |
| LIBID-OAUTH-003 | The ceremony caller may supply only the selected Chain Profile's exact 32-byte chain-ID hash, platform ID, exact 32-byte operation-domain hash, and transaction data. It cannot substitute server, client, redirect, ceremony version, authorization nonce/digest, notary, asset URL, or proof destination. |
| LIBID-OAUTH-005 | Redirect bootstrap bounds and copies both URL components and clears query/fragment before parsing, storage, rendering, errors, module fetch, or any connection, including malformed and oversized input. |
| LIBID-OAUTH-006 | The ceremony popup extracts exactly one syntactically valid `state` across the bounded query and fragment as `ceremonyId`, authenticates the retained opener source/origin through `AppAuthenticateOrigin`, and only then sends `PopupDeliverParams` with that ID and the unchanged inline `oauthReturn`. The application-scoped client claims the Ceremony mapped to that source, uses its retained platform/version parser to validate transport and fields and classify the outcome, and responds exactly once with `AppRequestProof` after success or parameterless `AppCancelCeremony` after denial or failure. It performs no storage lookup and does not expose callback values through the public API. Openerless, absent, duplicate, malformed, stale, replayed, post-reload, wrong-origin, wrong-ID, and wrong-version states restart OAuth. |
| LIBID-OAUTH-007 | The selected platform ceremony rejects the wrong transport, a nonempty disallowed transport, mixed transport, malformed encoding, duplicate authoritative fields, mixed success/error, wrong state/client/redirect/platform, and a Google ID Token at or after its signed `exp` before proving. Mutable X/GitHub proof lifetimes are tested at the Platform Verifier, not enforced by the browser. |
| LIBID-OAUTH-009 | The ceremony popup binds its coordinator iframe through the exact parent/child `WindowProxy` and browser-stamped origin. A fallback prover window clears its ceremony-ID fragment and uses the same-origin `BroadcastChannel` derived from that ID only with the coordinator, without relying on `window.opener`. Each is a one-shot ceremony channel and claims no separate prover-authentication boundary. |
| LIBID-OAUTH-010 | On `AppRequestProof`, the coordinator immediately checks `crossOriginIsolated` and `SharedArrayBuffer` before any credential-bearing network request. If qualified it proves locally; otherwise it retains the input and sends nonterminal `ProverRequestIsolation`. The fallback window checks the same properties before `ProverConfirmIsolation(ceremonyId)` and before receiving `AppRequestProof`; failure there sends terminal `AbortCeremony` with a sanitized string reason. Version-specific worker initialization follows the check, and later failure clears inputs and sends `AbortCeremony` rather than selecting fewer workers or an unisolated mode. |
| LIBID-OAUTH-011 | Before proof acceptance, the durable Job contains no authorization nonce or digest, code verifier, or provider-returned query or fragment. After URL clearing and the one-use ceremony-ID claim, those values remain memory-only. The bounded query and fragment cross the authenticated popup/application roundtrip; they and the derived code verifier then cross the popup/prover boundary only in `AppRequestProof`. They never enter IndexedDB, local/session storage, a later URL, progress, logs, traces, analytics, errors, or the public API. The authorization nonce remains in the Ceremony Client, crosses no prover message, and enters only the accepted `OAuthProof`, after proof delivery establishes that the token exchange completed. The application origin is trusted for transient `AppRequestProof` and provider-parameter handling; same-origin script isolation is not a protocol property. |
| LIBID-OAUTH-012 | The isolated prover sends one `ProverDeliverProof<unknown>` with a structured-clone proof; no proof-specific delivery message or payload discriminator exists. The live Ceremony supplies its selected platform and ceremony version to `platforms/index.validateProofMessage`, receives `ProverDeliverProof<ProofByPlatformVersion[P][V]>`, and combines that typed result with retained authorization and locally validated platform evidence to assemble the corresponding version-discriminated `OAuthProof` and non-authoritative Identity preview. A literal platform passed to `CeremonyClient.new` type-checks through `Ceremony<P>` to the matching `IdentityResult<P>`; a generic wrapper preserving `P` retains that platform union through `proveUserIdentity()`. A dynamic or deliberately widened `PlatformId` returns the mapped discriminated union, and narrowing both `oauthProof.platformId` and `oauthProof.platformCeremonyVersion` narrows its proof. Adding another platform/version module changes the closed aggregator output, not `OAuthProof` or CCDP. Neither prover nor Ceremony Client imports a final-proof verifier. X and GitHub assembly adds no chain ID, digest, extracted identity/client/verifier/time field, circuit public input, verifier address, or verification key. Callers cannot supply or override Identity fields; only Consumer verification makes them authoritative. |
| LIBID-OAUTH-013 | The ceremony popup accepts `ProverDeliverProof<unknown>` only after `AppRequestProof`, exact-matches its `ceremonyId`, and forwards its unknown structured-clone `proof` unchanged over the live authenticated app channel. `platforms/index` then dispatches the exact selected platform/version validator and enforces that version's proof shape and bounds: Google version `1` requires one bounded Honk proof and its named audience, subject, email, expiry, and signing-key-modulus fields with no attestation or flattened public-input array; X and GitHub version `1` each require their own proof type with one bounded Honk proof and exactly the ordered token and identity attestations. Wrong-shape, malformed, oversized, cross-platform, or cross-version values fail. Interruption before `proveUserIdentity()` assembles and returns an accepted `IdentityResult` requires fresh OAuth. |
| LIBID-OAUTH-014 | The live Ceremony accepts delivery only from its authenticated source/origin and exact internal ID, checks the `OAuthProof`, derives Identity with that same `OAuthProof` and authenticated client identifier, and resolves `proveUserIdentity()` with `{ status: 'accepted', identity }`. That status means provider approval and local checks succeeded, not Consumer acceptance. Wrong origin/ID, cancellation, or replay returns no accepted result. Only Consumer acceptance makes the preview authoritative. |
| LIBID-OAUTH-015 | The GitHub token endpoint is synchronous and stateless, with no action, proof job, polling, result, progress, cancellation, or retry record; launch exposes no browser TLS bridge route. |
| LIBID-OAUTH-016 | The live Ceremony freezes the `CeremonyConfig`-selected client and redirect plus the nullable code verifier derived from the Authorization Digest and `authorizationNonce` by the normative PKCE construction. Accepted `AppRequestProof` carries the frozen client, redirect, and derived verifier but not `authorizationNonce`. The authorization request serializes its existing `ceremonyId` unchanged as OAuth `state`; no separate OAuth-state or PKCE-randomness value exists. |
| LIBID-OAUTH-017 | Navigate the opener from allowed origin A to allowed origin B during OAuth. B may receive `PopupRequestAuthentication(version)` but receives no ceremony ID; missing, guessed, replayed, or wrong-ID `AppAuthenticateOrigin` releases no `PopupDeliverParams`. A valid authentication from the retained source succeeds only when its browser-stamped origin is allowed and its ID exact-matches OAuth state. Messages from unrelated `WindowProxy` sources are ignored. |
| LIBID-OAUTH-018 | A valid ceremony-bound provider denial in `PopupDeliverParams` makes the client resolve `proveUserIdentity()` with `{ status: 'denied' }` and send `AppCancelCeremony` for cleanup without exchange, proving, or Job access. The composition owns any resulting Job transition. Before `AppRequestProof`, the popup treats every `AppCancelCeremony` identically: it clears the retained query and fragment, attempts to close, and renders one fixed fallback message if closing fails. A failure after accepted OAuth rejects rather than resolving as denial. |
| LIBID-OAUTH-019 | A missing retained source, unknown ceremony, or wrong callback version sends no `AppAuthenticateOrigin` and releases no callback value. Malformed, mismatched, unsupported-ceremony-version, and later internal-validation failures return no Identity; after authentication and selection, the client sends `AppCancelCeremony`, which follows the same pre-prove cleanup path. A valid success atomically claims its live ceremony-map entry, sends `AppRequestProof`, runs one attempt, and permits no second `AppRequestProof`; cancellation or a racing terminal outcome makes a late result inert. |
| LIBID-OAUTH-020 | A provider callback with a missing or silent opener, or no valid `AppAuthenticateOrigin`, reaches the same fixed rejection at exactly `REDIRECT_OPENER_TIMEOUT_MS`, clears in-memory redirect bytes, severs any opener, and never hangs or navigates to attacker-controlled content. |
| LIBID-OAUTH-021 | `AppRequestProof` contains exactly `ceremonyId`, `platformId`, `platformCeremonyVersion`, `clientId`, `redirectUri`, bounded inline `oauthReturn`, and nullable `codeVerifier`. Its nested query and fragment are unchanged from `PopupDeliverParams.oauthReturn`; the popup byte-matches both echoed fields to its retained values before use and forwards `AppRequestProof` unchanged to the prover. Google requires null `codeVerifier`; X and GitHub require the exact 43-character value derived by the normative PKCE construction. The prover receives neither `authorizationNonce` nor the expected Authorization Digest: the Ceremony Client matches the verified Google nonce or X/GitHub verifier to its retained authorization after proof delivery. No broader authorization, OAuth wrapper, operation field, or application origin crosses this boundary. |
| LIBID-OAUTH-022 | Every member of the closed `CCDPMessage` union starts with its creating component—`App`, `Popup`, or `Prover`—and has the matching lower-kebab discriminator; shared popup/prover `AbortCeremony` is the only origin-prefix exception. `ProverPrefetchingAssets` contains exactly type, fixed `CCDPVersion`, ceremony ID, platform ID, and ceremony version; `PopupRequestAuthentication` contains only type and that version; `AppAuthenticateOrigin` contains only type and ceremony ID; `AppCancelCeremony` contains only type; `ProverRequestIsolation` contains only type; `ProverConfirmIsolation` contains only type and ceremony ID; and `ProverDeliverProof` contains only type, ceremony ID, and `proof: unknown`. `AbortCeremony` contains exactly type and a sanitized string reason. The client validates each application-facing popup version and never echoes or negotiates one. `PopupDeliverParams` contains exactly type, ceremony ID, and bounded inline `oauthReturn` with exact query and fragment fields. Mutate type, order, source, browser-stamped origin, version, profile, ID, `oauthReturn`, query, fragment, or later message fields—or add an asset URL, payload origin, client version, status, revision, counter, raw exception, or unknown envelope field—and require no state change. |
| LIBID-OAUTH-023 | Redirect-origin persistence contains no action, credential, token-exchange attestation, transcript, witness, code verifier, prover input, generated `OAuthProof`, verified result, or proof-delivery record. |
| LIBID-OAUTH-024 | Missing, canceled, wrong-ID, wrong-origin, unsupported-ceremony-version, and replayed live deliveries fail without returning Identity or reviving a Ceremony. |
| LIBID-OAUTH-025 | Destroy the application-side `Ceremony`, ceremony popup, coordinator iframe, isolated prover window, or application document after proof generation but before `proveUserIdentity()` resolves. No proof can be recovered; a new ceremony requires fresh OAuth. |
| LIBID-OAUTH-026 | An initial `/api/v1/ceremony/popup` launch fragment contains exactly ceremony ID, platform ID, and ceremony version and is cleared before subresources. The popup embeds the same tuple in its exact `/api/v1/ceremony/prover#prefetch(...)` child, which clears it before importing its root module or using the network, and forwards the child's matching `ProverPrefetchingAssets` unchanged to a server-allowed opener origin after only that profile's single flights exist or its bounded unavailable path ends. A missing/malformed profile or silent child fails before OAuth. The app exact-validates protocol version, popup origin, live profile/ID, and expected source when supplied; otherwise it binds the matching fallback source, then navigates it without replying. |
| LIBID-OAUTH-027 | `proveUserIdentity()` receives at most one caller-opened `expectedPopup`, opens no browsing context, runs the complete one-shot flow, and resolves with exactly one `IdentityResult`: accepted with a locally checked, non-authoritative Identity preview or denied. With an expected source it immediately navigates and pins that `WindowProxy`; without one it lets the real anchor navigate and binds the source of the matching initial `ProverPrefetchingAssets`. Any non-CCDP message is unknown on that channel. |
| LIBID-OAUTH-028 | The ceremony popup always forwards exactly one `AppRequestProof` to its coordinator iframe. A qualified coordinator proves locally. Otherwise it sends `ProverRequestIsolation`, accepts exactly one matching `ProverConfirmIsolation` from an isolated fallback window, and forwards its retained `AppRequestProof` once. The window returns zero or more events followed by proof or `AbortCeremony` through the coordinator; the coordinator and ceremony popup validate and forward them unchanged. The two placement messages never cross the application boundary or terminate the Ceremony. Invalid direction, order, replay, post-terminal traffic, unknown fields, wrong IDs, missing or non-string AbortCeremony reasons, and messages outside either one-shot channel change no state. |

## Deployment and server configuration

| ID | Assertion |
|---|---|
| KIT-001 | The deployment exposes stateless `GET /api/v1/ceremony/config`, fixed `GET /api/v1/ceremony/popup`, a byte-identical callback alias at the configured `redirectUri` path (default `/auth/v1/callback`), fixed `GET /api/v1/ceremony/prover`, and only the confidential routes required by enabled platforms, including `POST /api/v1/ceremony/github-token` for GitHub; it exposes no OAuth preparation route. |
| KIT-002 | Selecting GitHub requires the integrating server to implement and mount the normative fixed token endpoint and fails when its client secret, notary configuration, route, or immutable assets are absent. The ceremony package contributes only `platforms/github` browser request/response codecs and validation. |
| KIT-003 | Google or X Proxy requires no server ceremony state or confidential exchange route beyond public `CeremonyConfig` and fixed page/asset hosting; launch exposes no browser-MPC bridge route. |
| KIT-004 | Deployment `allowedAppOrigins` is a nonempty server-controlled list of canonical origins with no protocol maximum; membership uses `Set` semantics, so permutations and duplicates behave identically, each listed origin can fetch configuration and complete the same redirect flow, and an unlisted origin fails. The list is not part of public `CeremonyConfig`. |
| KIT-005 | Only the application-scoped client fetches `CeremonyConfig`, and the request requires its browser `Origin`. The same server configuration drives that CORS decision and the canonical `allowedAppOrigins` embedded into the byte-identical `/api/v1/ceremony/popup` and configured callback-alias responses; `startPopup(oauthReturn, allowedAppOrigins)` exact-validates the list and authorizes only browser-stamped `MessageEvent.origin`. Popup and prover perform no configuration request. Request `Origin`, `Referer`, query, fragment, and client messages cannot select or alter the embedded list. |
| KIT-006 | The registered redirect URI is canonical HTTPS on the configured server origin with no credentials/query/fragment; the explicit loopback profile is the only HTTP exception. Its developer-configurable path defaults to `/auth/v1/callback`. Both the default and a nondefault path directly serve bytes and security headers identical to `/api/v1/ceremony/popup` and never issue an HTTP redirect. |
| KIT-007 | `ceremonyId` is a fresh lowercase UUIDv4 string serialized unchanged as OAuth `state` and used once for browser continuity in `AppAuthenticateOrigin`; it is never a proof nonce or chain authorization. `authorizationNonce` is a fresh 32-byte random value and also supplies the PKCE derivation input. Only the derived code verifier crosses the prover boundary, and the nonce is published only in the accepted `OAuthProof` after the token exchange completes. No separate PKCE-randomness value exists. |
| KIT-008 | Reusing a live ceremony ID, or supplying malformed, padded, derived, wrong-version, wrong-variant, or wrong-case values, fails without weakening state or channel checks. |
| KIT-009 | Redirect/prover HTML and headers are byte-identical across requests; no request field changes CSP, script URL, origin, platform, or mode. |
| KIT-010 | The CSP-hashed inline redirect bootstrap performs only capture, clearing, integrity-pinned modulepreload/import, and exported-entrypoint invocation; it never interprets identity-platform fields or accesses storage/network before clearing. |
| KIT-011 | X/GitHub redirect queries are absent from server/proxy logs, traces, analytics, and error reports. |
| KIT-012 | The redirect HTTP response and `libid-ceremony-popup.js` perform no token exchange or server mutation. After URL clearing, opener/request authentication, one-use ceremony-ID claim, and transfer of the closed GitHub provider-return variant, only the isolated GitHub prover calls the fixed stateless token route. |
| KIT-013 | The normative `/api/v1/ceremony/github-token` route requires `Origin` equal to the redirect/prover origin, rejects cross-origin preflight and redirects, accepts only POST with no query and exact JSON `TokenRequest`, uses server-configured client/secret/redirect/notary settings, and returns only status 200 with exact JSON `TokenResponse`, whose `bearerBlinder` is canonical unpadded base64url decoding to exactly 16 bytes. GET, another method, another media type, or malformed JSON fails before token exchange. The server implements the endpoint; `platforms/github` owns only its browser codecs and validation. |
| KIT-014 | Token-exchange mutation, timeout, restart, duplicate request, and response loss retain no server state. The valid bounded response remains browser memory-only; loss before `proveUserIdentity()` returns requires fresh OAuth. |
| KIT-015 | No API input, request, user control, retry, or browser probe selects browser-MPC transport; every launch X and GitHub ceremony uses the deployed Proxy transport. The notary itself opens the fixed platform socket, and no application, request, redirect configuration, or prover message supplies its proxy, socket, destination, or routing. |
| KIT-016 | The deployment serves `libid-ceremony-prover.js` from the redirect origin so the same URL can run as a module service worker; build-pinned external workers/WASM and configured libID assets still pass integrity/CORS and isolation loading. Replacing either source is a full code-supply-chain trust choice. |
| KIT-017 | A mutable root-module URL, missing SRI, broad script/connect source, request-derived CSP, or asset redirect is rejected as a production deployment. |
| KIT-018 | Redirect/prover pages are served from a dedicated cookie-free origin with no unrelated same-origin API in the reference deployment. |
| KIT-020 | The ceremony requires no canonical orchestration service, preparation endpoint, continuation route, status server, server action ledger, or proof-recovery service. Its server surface is public `CeremonyConfig`, fixed browser documents/assets, and any fixed confidential platform routes. |

## Browser isolation and response policy

| ID | Assertion |
|---|---|
| CSP-001 | `/api/v1/ceremony/popup` and its direct configured callback alias (default `/auth/v1/callback`) serve the same top-level, non-isolated, no-store/no-referrer, non-frameable response and preserve the application opener. |
| CSP-002 | The single prefetch/proving `/api/v1/ceremony/prover` response sends DIP isolate-and-require-corp, COOP same-origin, COEP require-corp, no-store/no-referrer, and same-origin-only framing for DIP. COOP severs only the top-level popup opener and does not create a separate service-worker registration or cache. |
| CSP-003 | Both documents use `default-src 'none'`, `object-src 'none'`, `base-uri 'none'`, `form-action 'none'`, no broad `https:`, and no JavaScript `'unsafe-inline'`/`'unsafe-eval'`; one exact CSP hash permits only the deployment-generated inline bootstrap. |
| CSP-004 | Route headers and asset URLs are fixed deployment values; hostile query/fragment/origin strings cannot inject or expand CSP. |
| CSP-005 | The redirect bootstrap records query/fragment transport, clears both URL components, and makes no storage/network/module action while raw redirect bytes remain in the URL. |
| CSP-006 | `/api/v1/ceremony/prover` accepts only an empty query and one of three closed fragments: `prefetch(ceremonyId, platformId, ceremonyVersion)` for prefetch, empty for the callback coordinator, or a bare ceremony ID for the fallback window. It captures and clears the fragment before appending `modulepreload` or opening storage, `BroadcastChannel`, worker, or network. |
| CSP-007 | In Chromium, Firefox, WebKit, Pixel emulation, and iPhone WebKit emulation, each bootstrap appends the exact immutable root-module URL with SRI and `crossorigin=anonymous`, waits for `load`, imports the identical URL once, and reaches the exported entrypoint; preload failure, a second root fetch, an ambient-global handoff, wrong MIME, missing `nosniff`, CORS failure, or COEP incompatibility fails. |
| CSP-008 | Direct cross-origin Worker construction fails; the allowed local blob bootstrap imports only the immutable code-pinned worker module. |
| CSP-009 | Spawner workers permit only required `blob:`/worker/connect sources; leaf workers use `worker-src 'none'`; every worker has its own CSP. |
| CSP-010 | Real nested bb.js workers and WASM load only from their code-pinned locations, while TLSNotary workers and WASM load only from their integrity-bound deployment locations; both pass through the recursive bridge. No same-origin mirror of either graph is required. |
| CSP-011 | The byte-identical prover response has one deployment-fixed `connect-src` containing the closed union of exact origins required by every enabled platform/version profile; no query, fragment, message, or selected profile changes it, and it contains no broad scheme or TLS bridge origin. The active platform's closed network catalog contains only its own exact configured or code-pinned URLs: X and GitHub use their pinned Proxy-notary origins, GitHub additionally uses its same-origin token route, and Google uses its pinned JWKS origin. Caller input cannot select another profile's CSP-permitted origin. The union is a ceiling for the trusted prover bundle, not browser-enforced cross-profile compartmentalization. |
| CSP-013 | The redirect creates one same-origin `/api/v1/ceremony/prover` iframe during prefetch and sends `ProverPrefetchingAssets` after its bounded startup barrier. After OAuth, a newly loaded iframe remains the coordinator through either proving placement. The popup sends it `AppRequestProof` once; it checks DIP isolation before network use and either proves locally or sends `ProverRequestIsolation` to expose the user-opened top-level fallback. The non-isolated redirect never navigates itself into isolation. |
| CSP-014 | The isolated prover window has zero reliance on opener. Its cleared fragment contains only the ceremony ID used to derive its one-shot coordinator `BroadcastChannel`; `ProverConfirmIsolation`, `AppRequestProof`, and the delivered proof exact-match that ID while AbortCeremony and advisory events do not repeat it. No role, credential, or proof bytes enter its URL, and the protocol claims no authority beyond the trusted same-origin deployment. |
| CSP-015 | Every content-addressed prover, worker, and WASM response uses exact MIME, `nosniff`, CORS/CORP compatible with isolation, and `Cache-Control: public, max-age=31536000, immutable`; retained entries for the exact release serve the warm path. |
| CSP-016 | Every launch profile runs its exact multithreaded real-prover configuration in both qualified DIP and popup placements. Immediately after `AppRequestProof`, missing iframe isolation or shared memory produces `ProverRequestIsolation`, not `AbortCeremony` or a network request. The top-level prover checks both properties before sending `ProverConfirmIsolation` and receiving the retained input; failure there sends terminal `AbortCeremony` with a sanitized string reason. Missing required workers or nested-worker startup later also sends `AbortCeremony`, clears inputs, and never selects a single-threaded or unisolated fallback. |
| CSP-017 | Scripted and real-anchor launch both reach `/api/v1/ceremony/popup#launch(ceremonyId, platformId, ceremonyVersion)`, which clears the fragment, loads `/api/v1/ceremony/prover`, and requests only that profile. The popup forwards matching `ProverPrefetchingAssets` once its selected single flights exist or the bounded unavailable/registration-failure path ends, without waiting for downloads. The application exact-validates protocol/profile versions, ID, source, and popup origin, retains the source, then immediately navigates it to the provider without replying. No developer/application switch, prefetch route, mode flag, or separate artifact exists. Cold and warm X paths meet its code deadline. |
| CSP-018 | The exact same-origin `libid-ceremony-prover.js` URL loaded by `/api/v1/ceremony/prover` runs as both Window module and module service worker with scope covering `/api/v1/ceremony/`. The Window branch exact-validates the server-embedded libID-asset manifest and resolves one profile with the code-pinned toolchain assets. The worker verifies each deployment asset against its code-pinned digest before caching it and admits only the build's exact toolchain resources; CRS work can call only the pinned bb.js loaders with the package's fixed launch-wide SRS size and completes through bb.js's IndexedDB cache. Unknown profiles/origins/URLs, redirects outside the manifest or code-pinned set, opaque or partial responses, mutable aliases, wrong bytes, and fragment- or message-supplied assets or SRS sizes fail without entering either cache. |
| CSP-019 | Exercise cold, partially evicted, fully warm, concurrent-ceremony, cross-platform shared-artifact, reload, worker-termination, quota/fetch failure, and unsupported-worker paths in both DIP and COOP-window placements. The worker owns each selected fetch from its first byte, fetches only missing canonical ordinary URLs, and makes later requests join its Cache Storage single flight or the fixed launch CRS single flights. OAuth navigation and COOP do not duplicate or partition that work; completed CRS is read from bb.js's IndexedDB cache, and unselected profiles fetch nothing. New documents reconnect instead of awaiting a destroyed document's Promise; completed caches need no separate durable completion marker. Failure follows the identical selected-profile cold path without weaker isolation, fewer threads, or correctness dependence on prefetch. |

## Browser lifecycle and progress

| ID | Assertion |
|---|---|
| LIBID-BROWSER-001 | `Ceremony.navigation` supplies one `/api/v1/ceremony/popup` `href` whose closed fragment contains only ceremony ID, platform ID, and selected ceremony version, plus the exact non-reserved `libid-${ceremonyId}` target. The caller synchronously opens `about:blank` with that target and neither `noopener` nor `noreferrer`; a null script result leaves anchor navigation untouched, while `proveUserIdentity({ expectedPopup })` navigates a successful handle to the same `href`. |
| LIBID-BROWSER-002 | Strictly isolated application pages fail before OAuth navigation. A removed opener, framing, synthetic/delayed launch, or opaque sandbox origin fails before callback credentials are used or proving starts. |
| LIBID-BROWSER-003 | The ceremony reuses one popup through shared prefetch, identity-platform authorization, and redirect execution. The client retains either the non-null scripted-open handle or the exact browser-stamped source learned from the matching initial `ProverPrefetchingAssets`; after OAuth, only that source can cause `AppAuthenticateOrigin(ceremonyId)`. Proving adds one redirect-owned coordinator iframe; the documented fallback may temporarily add one isolated prover window. |
| LIBID-BROWSER-004 | Each target behaviorally selects DIP only when the child is isolated with shared memory and otherwise exposes the isolated-popup fallback; Chromium, Firefox, WebKit, Pixel emulation, and iPhone WebKit emulation pass their selected path without user-agent sniffing. |
| LIBID-BROWSER-005 | The isolated prover window is opener-independent; `ProverConfirmIsolation`, `AppRequestProof`, advisory events, `AbortCeremony`, and proof delivery use its coordinator's same-origin channel derived from the exact ceremony ID. Concurrent or stale ceremony IDs do not mix. |
| LIBID-BROWSER-006 | The Ceremony client reports only `authorization`, `oauth-validation`, and `proof-generation`, entering the last immediately before it sends `AppRequestProof`; that stage includes isolation selection and any fallback user activation. Platform modules report only opaque steps and cannot select or infer a common stage. `ProverNotifyEvent` carries only the platform step and prover-stamped time through the prover and popup; the authenticated client accepts it only during its locally known `proof-generation` stage, preserves the time, and publishes `CeremonyEvent`. It rejects unauthenticated or illegal transitions. No composition-owned preparation, confirmation, submission, or finality stage enters either event. |
| LIBID-BROWSER-007 | Google, X, and GitHub relay post-isolation progress through active prover → coordinator iframe → redirect → app with no server status relay, and each enabled platform/version emits its exact version-owned diagnostic-span catalog and lifecycle; missing live relay leaves the prover window visibly authoritative for UX only. |
| LIBID-BROWSER-008 | One live ceremony popup serializes progress messages. Duplicate, missed, or artificially reordered delivery affects UX only and cannot authorize, cancel, alter, or complete a ceremony. Channel loss provides no ceremony recovery. |
| LIBID-BROWSER-009 | Popup closure is neither success, denial, nor cancellation. Both launch paths retain the popup handle after `ProverPrefetchingAssets`; `popup.closed` is only an advisory retry hint. Closure before fallback source binding has no observable handle. A lost live ceremony returns no Identity and requires fresh authorization; downstream consumer state is outside this package. |
| LIBID-BROWSER-010 | In cold and verified-warm profiles, the complete request direction of X's first notarized token session reaches X before the authorization-code deadline. Delaying completion of that request direction past the deadline abandons the ceremony; response receipt, the identity session, proving, and delivery may complete later. |
| LIBID-BROWSER-011 | Real-device tests cover each platform app installed/absent, authentication in app/browser, approval/denial, suspension, ignored close, and same-tab/openerless return. |
| LIBID-BROWSER-012 | Openerless/new-profile return fails as restart without consuming another ceremony or claiming cross-profile recovery. |
| LIBID-BROWSER-013 | Race `ceremony.cancel()` against live Identity delivery in both orders. Cancellation sends authenticated parameterless `AppCancelCeremony`; the ceremony popup always forwards it to the coordinator iframe, which cancels local work or relays it to the fallback window. The window attempts to close itself, the popup removes the coordinator, and prover workers/connections stop and inputs clear without an acknowledgement or platform-specific cancel path. A cancellation which wins returns no Identity, and a late result cannot revive the Ceremony. |
| LIBID-BROWSER-014 | Launch two concurrent ceremonies and require distinct target names and `WindowProxy` values; navigation or fallback for either ceremony cannot replace the other's popup. The prover window remains outside this target contract. |
| LIBID-BROWSER-015 | After OAuth, fallback proving is never opened automatically. `ProverRequestIsolation` from the exact coordinator exposes a real **Continue proving** anchor; its user activation opens exactly one isolated window even where Firefox blocks a delayed scripted open. The window must send one matching `ProverConfirmIsolation` before the coordinator forwards its retained `AppRequestProof`. |
| LIBID-BROWSER-016 | The callback redirect owns the coordinator iframe for its lifetime, and that iframe owns either local DIP proving or the fallback-window relay; no persistent prover runs under the original app. Background the app while the redirect is foregrounded on mobile Chromium and require forward progress in the visible redirect-owned placement despite a capability-equivalent app child being throttleable. |
| LIBID-BROWSER-018 | Closing consent without a returned OAuth error emits no denial or terminal result; a valid ceremony-bound provider denial resolves immediately as `{ status: 'denied' }`. `cancel()` opens no auxiliary context and best-effort stops reachable live work. `libid-ceremony-popup.js` owns no GitHub server request; a remote stateless request may finish but no response is used. |
| LIBID-BROWSER-019 | Real-device qualification separately covers iOS memory pressure, suspension/eviction, browser chrome, popup blocking defaults, native-provider handoff, and background scheduling; Playwright WebKit/iPhone emulation cannot satisfy these gates. |
| LIBID-BROWSER-020 | **Restart authorization** never opens a clean redirect or proof-recovery context; when the live ceremony is gone the caller creates a fresh `Ceremony` and uses its initial real-anchor/synchronous-open launch with fresh OAuth. |
| LIBID-BROWSER-021 | Reload destroys the in-memory ceremony map and every live Ceremony. No redirect, progress record, stale popup handle, or prior ceremony ID is presented as resumable or can recover proof delivery. |
| LIBID-BROWSER-022 | Destroy and recreate the application-side `Ceremony`, ceremony popup document, coordinator iframe, and isolated prover window at each point in the live flow. No advisory progress, stale popup handle, or recreated context resumes OAuth, proving, or proof delivery. |
| LIBID-BROWSER-023 | Exercise featureful and featureless auxiliary launch across touch, pointer, desktop-mode, and tablet-with-trackpad cases. Browser promotion between popup and tab changes presentation only; no user-agent/input heuristic, integrator mode, or observed presentation changes security, storage, recovery, or prover placement. |

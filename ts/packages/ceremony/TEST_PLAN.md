# `@libid/ceremony` test plan

This plan tests the `@libid/ceremony` package architecture in
[ARCHITECTURE.md](ARCHITECTURE.md) and its browser protocol in
[CCDP.md](CCDP.md), its deployment in [CCDP_DISTRIBUTION.md](CCDP_DISTRIBUTION.md), its
proof-generation subsystem in [PROVING.md](PROVING.md), and the [OAuth bridge
contract](OAUTH_BRIDGE.md).
Generic popup creation, connection, carrier, continuity, control, and local
diagnostic behavior is qualified by the
[popup package test plan](../popup/TEST_PLAN.md). Rows here test only ceremony's
use of that package.
Normative proof and platform behavior come from the
[common ceremony rules](../../../specs/ceremony-common.md) and
[identity-platform ceremonies](../../../specs/platform-ceremonies.md).

Every row is one stable requirement and may require multiple positive and
negative cases. Tests **must cite its ID**. Existing IDs retain their names;
never renumber or reuse one.

A row belongs here only when it exercises `@libid/ceremony`, an emitted browser
asset, or a server/browser contract declared by the package. Composition-owned Job
transitions, wallet preparation or confirmation, transaction submission, and
post-ceremony recovery belong to their respective composition test plans.

## Package and asset isolation

| ID | Assertion |
|---|---|
| LIBID-MOD-002 | `@libid/ceremony/prefetch` supplies the entry code embedded by the versioned Prefetch document and the module Service Worker served at `/ccdp/v{CCDPVersion}/worker.js`. Its Window branch owns top-level selected-profile prefetch; its Worker branch composes only the `@libid/popup/worker` continuity handler, ordinary immutable-asset Cache Storage single flights, and bb.js CRS-loader single flights into bb.js's IndexedDB cache. `@libid/ceremony/airlock` and `@libid/ceremony/prover` separately supply the entry code embedded by Airlock and the active isolated Prover. Each document passes the appropriate popup admission and optional fallback through to `PopupConnection.accept`; none contains a document-role branch. Their bundles contain no final-proof verifier, callback, React/UI framework, wallet key, connector, Registry submission, or wallet-policy code. |
| LIBID-MOD-003 | The CCDP Distribution serves the `@libid/ceremony/callback` entrypoint at `/ccdp/v{CCDPVersion}/callback.js` with every normal Callback view, the inline libID logo, and exact stylesheet text. OAuth Bridge HTML contains only its security bootstrap and empty mount point; no separate logo, stylesheet, UI template, theme, renderer, or application customization input exists. The implementation contains no proving code, React/UI framework, wallet key, connector, Registry submission, or wallet-policy code. |
| LIBID-MOD-011 | `@libid/ceremony/client` exact-validates one `CeremonyConfig` record, including its canonical CCDP origin and each nonempty duplicate-free `ceremonyVersions` list, and fetches it once per client. CCDP [resources](CCDP.md#documents-and-routes) never fetch it. |
| LIBID-MOD-012 | The Application opens `/ccdp/v{CCDPVersion}/prefetch`, whose embedded entry code owns selected-profile fetching and the shared Worker. After OAuth, Callback navigates through `/ccdp/v{CCDPVersion}/airlock` to `/ccdp/v{CCDPVersion}/prover`; Airlock establishes the CCDP-origin carrier, the Worker preserves it into Prover, and Prover emits fieldless `ProverReady` before accepting proof input. All three CCDP Distribution documents and the Worker use one compatible package release. |
| LIBID-MOD-013 | `@libid/ceremony` contains no OAuth bridge handler or secret. The GitHub version's prover leaf implements only the bounded `TokenRequest` and `TokenResponse` codecs and validation fixed by `OAUTH_BRIDGE.md`; ccdp, client, callback, airlock, prover, worker, platform, and browser exports build with the OAuth Bridge implementation unavailable. |
| LIBID-MOD-014 | The ceremony client's public operation input contains exactly a 32-byte `chainId` hash, enabled `platformId`, 32-byte `operationDomain` hash, and bounded opaque `transactionData`. It rejects unknown fields, malformed hash lengths, ceremony version, authorization nonce, authorization digest, OAuth client, redirect, proof-domain, Registry, or finalizer values. |
| LIBID-MOD-015 | For every `CeremonyClient.new`, the client exact-validates and copies the supplied chain and operation-domain hashes without deriving them, selects the numerically greatest `PlatformCeremonyVersion` present in both its closed local catalog and that platform's `CeremonyConfig` independent of list or object-key order, draws a fresh cryptographically secure 32-byte authorization nonce, reproduces the normative authorization digest and code verifier, and freezes every input and derived value before OAuth-platform navigation; later caller mutation changes nothing. |
| LIBID-MOD-016 | `@libid/ceremony/ccdp` builds without platform, browser, network, storage, Job, wallet, or UI code. All CCDP participants share its record types and decoder companions, use `@libid/popup` for typed delivery, and enforce ceremony order themselves. `ProverNotifyEvent` contains only type, bounded `PlatformStep`, and prover timestamp; the client alone adds one of the three common stages to `CeremonyEvent`. Generic connection and carrier behavior is absent from the ceremony package. |
| LIBID-MOD-017 | One application-scoped `CeremonyClient` fetches and validates `CeremonyConfig`, excludes configured platforms missing from its closed implementation or sharing no ceremony version with it, and creates independent stateful ceremonies through `new(ceremonyId, { connection, ...input })`. Each Ceremony owns its connection handlers and one-shot state; the client keeps no ceremony-ID routing table. Calling `new` for an excluded platform fails before launch. No package-global singleton, carrier constructor or options, generic Host, mutable connection setter, Job-store dependency, Registry callback, or composition plugin exists. |
| LIBID-MOD-018 | A created `Ceremony` retains the caller-constructed `PopupConnection` and exposes only its fixed `launchUrl`, event subscription, argument-free `proveUserIdentity`, and best-effort `cancel`; authorization is not exposed as a separate instance property. `launchUrl` is the frozen `${ccdpOrigin}/ccdp/v{CCDPVersion}/prefetch` URL with a closed ceremony-ID/platform-ID/platform-ceremony-version fragment. The caller owns the browsing-context target, while the popup package owns synchronous creation, native-anchor fallback, connection authentication, navigation, and closure. Ceremony exposes no public platform authorization URL, asset path, popup target or handle, carrier option, or mutable connection setter. |
| LIBID-MOD-019 | One closed client-safe catalog derives platform IDs, supported versions, and proof types from version validators without importing prover leaves. `validateProofMessage` performs the selected runtime check before narrowing; any TypeScript assertion stays behind it. The prover dispatch is exhaustive over the same keys. Adding a key requires its client, types, validator, and prover leaf; no second union/map, proof-version axis, or dynamic registration exists. |
| LIBID-MOD-020 | Each `CeremonyClient.enabledPlatforms` is an immutable intersection of `supportedPlatforms` and validated `CeremonyConfig` keys having at least one mutually supported ceremony version, in catalog order. It exposes no OAuth client, ceremony version, server configuration, or display metadata, and mutating a returned value cannot change ceremony admission. |
| LIBID-ASSET-001 | The target-neutral CCDP artifact pipeline derives first-party filenames and dependency edges from compiler/bundler output and each non-imported proving resource from its single owner-declared logical role. One code-owned response-profile table supplies routes and fixed policy; emitted bytes and paths supply CSP hashes/sources and media-specific values. Rename an internal entry, chunk, Worker, or emitted WASM: the validated graph and references update without another filename list, generated-source scrape, or route edit. Immutable source downloads are reused. A missing referenced body, unindexed static dependency, malformed external pin, mutable asset path, or partial graph fails before any deployment target receives it. The graph has no serialized public or deployment-facing format. |
| LIBID-ASSET-002 | After URL clearing, the Callback bootstrap imports its exact `{ccdpOrigin}/ccdp/v{CCDPVersion}/callback.js` implementation once, while the versioned Prefetch, Airlock, and Prover documents run their embedded entry code without another entry-script request. Import, media-type, CORS, or embedded-code failure stops before protocol execution. |
| LIBID-ASSET-003 | The ceremony build pins its Noir and bb.js JavaScript, workers, WASM, CRS paths, shared notarization-client paths, per-platform/version circuit paths, and one shared Notary Service address. That address defaults to `https://notary.lib.id`; an exact canonical HTTPS `LIBID_NOTARY_ADDRESS` may replace it only while building the static distribution and must also appear in the emitted Prover response policy. The CCDP Distribution serves the resulting asset graph from implementation-private immutable URLs on `ccdpOrigin`. Ceremony, opener, runtime deployment configuration, query, callback, message, and application inputs cannot replace a dependency, asset path, or Notary Service endpoint. |
| LIBID-ASSET-004 | A `PlatformCeremonyVersion` owns one platform's authorization-digest construction, OAuth grammar, proof construction, proof type and validator, and final `OAuthProof` assembly; the ceremony client selects the numerically greatest locally supported version advertised by the platform configuration and freezes it in the live Ceremony, `AppRequestProof`, and accepted `OAuthProof`. `platforms/authorization` supplies shared digest and PKCE helpers but has no independent version or mandatory policy: each platform-version slice owns whether and how it uses them. Chain, Registry, and contract-verifier versions do not affect that selection, and distinct Consumers may accept the same output. There is no independent proof-version axis. |
| LIBID-ASSET-005 | Changing one platform ceremony version does not version another platform or `CCDPVersion`; application input cannot select it, and `CeremonyConfig` only constrains selection to its advertised set. |
| LIBID-ASSET-006 | Every live Prefetch, Callback, Airlock, and Prover page retains the modules it loaded; the client dispatches the ceremony's exact ceremony version and never resolves `latest` during a ceremony. |
| LIBID-ASSET-007 | Every live `Ceremony` and `AppRequestProof` pin the exact public `PlatformCeremonyVersion`; the client and prover artifacts retain every version they still accept during their compatibility window and never resolve `latest` after creation. The OAuth Bridge shell validates generic Callback bootstrap input, while Callback owns only port acceptance and CCDP behavior. An unsupported version cannot be consumed and requires a fresh ceremony. |
| LIBID-ASSET-008 | The OAuth Bridge advertises only platform/version pairs present in its configured CCDP Distribution; no shared deployment system is required. Prefetch and Prover use one code-pinned asset tree: X and GitHub share the notarization-client and circuit paths, while every selected profile fetches only its own set. No runtime or browser asset-source configuration or catalog response exists, and launch does not runtime-hash assets. |
| LIBID-ASSET-009 | A real same-origin worker graph passes with every nested bb.js/TLSNotary worker and no cross-origin worker bridge or unisolated fallback. |
| LIBID-ASSET-010 | Rotate `CeremonyConfig` while a ceremony is live: the browser continues with the exact client ID, redirect URI, CCDP origin, and ceremony version frozen at creation; a bridge exchange which no longer supports that flight fails closed, and a fresh `CeremonyClient` uses current configuration. CCDP [resources](CCDP.md#documents-and-routes) perform no mid-ceremony configuration fetch. |
| LIBID-ASSET-011 | Every profile resolves the same code-pinned ACVM, Noir ABI, bb.js prover assets, and launch-wide 2^18-point BN254 SRS plus fixed Grumpkin data. X and GitHub additionally select the same code-pinned notarization client and `bearer-link` circuit; Google selects its code-pinned `oidc_google` circuit. The first platform starts the fixed SRS flight set, and every later platform reuses it without a size upgrade or refetch. Only missing profile-specific assets are fetched. |
| LIBID-ASSET-012 | A cleared `/ccdp/v1/prefetch#ceremonyId=<uuid>&platformId=<id>&ceremonyVersion=<uint>` document runs the CCDP v1 Prefetch implementation, whose Window branch and Worker fetch only the exact selected ordinary-artifact profile and start the fixed bb.js CRS loaders. Importing bb.js alone fetches no CRS. The fragment cannot supply a CCDP version, document role, asset path, or SRS size. Profiles for other enabled platforms remain unfetched; a later `AppRequestProof` selects the same profile and cannot widen it. |
| LIBID-ASSET-013 | Circuit-release conformance records the pinned EVM circuit sizes and actual SRS floors: `bearer_link` 42,006 fails at its mathematical 2^16 ceiling because bb.js 5.2 requires a positive multiple of its 4 MiB compressed-input chunk and succeeds at 2^17; `oidc_google` 179,443 succeeds at 2^18 and fails at 2^17. The checked-in launch-wide SRS constant is 2^18 and every profile proves under it. Browser user agent and deployment data cannot alter it. A changed circuit or bb.js requirement exceeding that constant fails the build. Splitting the fetch size later requires performance evidence, not a platform ceremony version change while proof semantics remain identical. |
| LIBID-ASSET-014 | Stage and promote a CCDP Distribution with one unchanged proving asset and one changed WASM body. The promoted distribution is graph-complete; the unchanged asset retains its URL and warm-cache reuse, the changed bytes use a new URL, and both old and new URLs remain retrievable through the compatibility window. Prefetch waits for the newest installing or waiting Worker to become active rather than dispatching to the stale active Worker, and activation does not proactively purge reusable immutable Cache Storage or bb.js CRS entries. |
| LIBID-ASSET-015 | The versioned Prefetch, Airlock, and Prover paths select one CCDP implementation before package code runs; OAuth `state` independently selects the matching `{ccdpOrigin}/ccdp/v{CCDPVersion}/callback.js`. The Callback shell keeps one current default input tuple, a closed supported-version list, and only necessary per-version input overrides. It invokes `startCallback(locationInput, ...(callbackInputOverrides[version] ?? defaultInputs))` without interpreting the tuple, and the selected implementation exact-validates its arguments. Before an incompatible default change, every affected older version receives its previous tuple as an override. Fragments and CCDP messages do not repeat selection. The OAuth Bridge API namespace, `PlatformCeremonyVersion`, and popup `ConnectionVersion` remain independent. |

## Platform proving

| ID | Assertion |
|---|---|
| LIBID-PROVER-001 | Every enabled profile maps in closed code to one circuit release artifact and compatible Noir/ACVM and bb.js versions, while the package supplies the same 2^18-point BN254 SRS and fixed Grumpkin data to all of them. A real browser UltraHonk proof for every profile generated with that SRS verifies under the release's key. Changing circuit bytes, ABI, public-input order, proof system, or bb.js compatibility without the required platform ceremony version update fails; changing the shared SRS fetch policy alone does not require one while proof semantics remain identical. No application, callback message, user agent, request value, or Ledger Verifier contract selects any of them. |
| LIBID-PROVER-002 | Google v1 emits its defined spans, strictly parses the ID Token/JWK, builds the private witness, and delivers bounded `GoogleProofV1` semantic fields without attestation or flattened inputs. The client validates shape/audience and derives the preview without verifying the proof. The pure adapter packs the digest and named fields into the exact 56 verifier inputs; ordering, length, packing, and one-byte mutations are vector-tested. |
| LIBID-PROVER-003 | X v1 emits its defined spans, notarizes token before identity, validates the bounded printable bearer, commits it with independent 16-byte blinders, reveals only the prescribed ranges, and proves `bearer-link`. It delivers one bounded Honk proof plus byte-exact token and identity attestations in order, with no sidecar or public-input array; commitments reconstructed from those attestations verify the proof. |
| LIBID-PROVER-004 | GitHub v1 emits its defined spans and exact-validates the server token response, attestation correlation, request bindings, layout, live fields, and bearer opening before `/user`; failure discards it. It then notarizes `/user`, proves shared `bearer-link`, and delivers one bounded Honk proof plus ordered byte-exact attestations. Local notary-signature verification is optional; downstream verification is authoritative. |
| LIBID-PROVER-005 | The X and GitHub profiles point to the same immutable `bearer-link` circuit path and resolve the same CCDP-origin notarization-client module/WASM pair and code-pinned prover toolchain. Cross-platform prefetch reuses those cache entries; platform-specific OAuth, server exchange, transcript construction, parsing, and progress remain in their respective closed modules and cannot alter the circuit ABI. |
| LIBID-PROVER-006 | The selected notary release supplies sibling `tlsn_wasm.js` and `tlsn_wasm_bg.wasm` files built from the same notary release, including `set_progress_callback`, with the worker bootstrap embedded in the module. X and GitHub use that pair as independent immutable responses; Google neither fetches nor initializes it. A missing release member fails before a notarized request. |
| LIBID-PROVER-007 | Every successful run emits one `started` and one `completed` for every diagnostic span, including cache hits. Every span started before a failure emits exactly one terminal event, while later spans are absent. Parent spans contain their children, documented dependencies hold, and otherwise-overlapping spans need not form a total order. X proves that `identity-session` starts only after token notarization returns the bearer. Prover timestamps permit per-span duration diagnostics without exposing URLs, credentials, witnesses, proofs, or raw errors. |
| LIBID-PROVER-008 | Only X token, X identity, and GitHub identity call the internal notarization adapter. Callers supply an exact request and deterministic reveal selector; the adapter enforces 4-KiB sent/32-KiB received ceilings, derives the fixed Proxy WebSocket, and accepts one bounded final attestation frame after TLSNotary releases the channel. It rejects malformed framing, truncation, overflow, duplicate output, redirects, and transcript-limit overflow. Only the required bearer/opening/attestation survives each call. |
| LIBID-PROVER-009 | Before the notarization wrapper returns, every raw TLSNotary commitment and opening maps exactly once by direction, range, and value to the read-only decoding of the original signed attested-data bytes. For each X and GitHub token/identity pair, the uniquely framed bearer commitments equal `SHA256(accessToken || bearerOpening)` and their matching TLSNotary hashes, and the two openings are independently generated exact 16-byte values. Wrong direction, shifted range, changed hash, changed opening, duplicate or missing commitment, malformed layout, or correlation against re-encoded rather than original bytes fails. |
| LIBID-PROVER-010 | The read-only decoder reproduces every field and the pinned digest of the upstream `libid-rs` fixture referenced by NOTARIZATION.md. Valid one-byte mutations change the field/digest; malformed mutations, truncation, trailing or reordered bytes, oversized counts, overflow, invalid ranges, and alternate encodings fail. Received signed bytes are never re-encoded. |
| LIBID-PROVER-011 | Every platform/version progress catalog assigns a bounded package-owned display label to each code and nonnegative weights to leaf spans only. Parent weight is zero and leaf weights form one positive total. For every valid serial and parallel completion order, emitted progress equals `0.95 * completedWeight / totalWeight`, remains finite and monotonic in `[0, 1)`, does not advance on `started`, and never double-counts nested spans. A cache hit follows the same events; failure invents no later progress; `ProverDeliverProof` alone renders completion. Labels and weights contain no caller, credential, identity, URL, proof, witness, or raw error data. |

## Ceremony client and Ceremony Cross-Document Protocol (CCDP)

| ID | Assertion |
|---|---|
| LIBID-OAUTH-001 | The application-scoped client fetches and exact-validates OAuth-bridge-owned `CeremonyConfig` once; unknown record fields, missing or wrongly typed values, noncanonical, empty, or out-of-range client, redirect, CCDP origin, known-platform, or ceremony-version values, and duplicate ceremony-version entries fail before navigation. `allowedAppOrigins` is rejected as an unknown public field. An unknown platform entry is ignored; an unknown but valid ceremony version is ignored when another advertised version is locally supported; a platform with no local implementation or common version is unavailable. CCDP [resources](CCDP.md#documents-and-routes) do not fetch configuration. |
| LIBID-OAUTH-002 | The frontend configuration request requires an exact browser `Origin` and matching noncredentialed CORS response. One request-invariant CCDP Distribution serves independently operated OAuth Bridges without registering them. Its Prefetch, Airlock, and Prover use `allowedApplicationOrigins: '*'`, accepting any valid browser-observed HTTPS Application origin and pinning the exact origin/source while the Application exact-authenticates `ccdpOrigin`; this grants only public prefetch, carrier continuity, and processing of that peer's own proof request. Callback releases `CallbackDeliverParams` only through a connection authenticated for the exact app origin allowed by its containing Bridge and `ceremonyId`; another Bridge's allowlist grants nothing. Request `Origin`/`Referer` and payload fields are not substitutes for popup-package authentication. |
| LIBID-OAUTH-003 | The ceremony caller may supply only the selected Chain Profile's exact 32-byte chain-ID hash, platform ID, exact 32-byte operation-domain hash, and transaction data. It cannot substitute OAuth Bridge, CCDP origin, client, redirect, ceremony version, authorization nonce/digest, notary, asset path, or proof destination. |
| LIBID-OAUTH-005 | Callback bootstrap bounds and copies both URL components and clears query/fragment before parsing, storage, rendering, errors, module fetch, or any connection, including malformed and oversized input. |
| LIBID-OAUTH-006 | Callback extracts one valid OAuth state and releases the unchanged `oauthReturn` without repeating the ID only through the accepted popup connection. The Ceremony bound to that connection exact-matches the returned state to its frozen CCDP version and ceremony ID and consumes it once while Callback navigates through Airlock to Prover. A valid acceptance waits for fieldless `ProverReady` before `AppRequestProof`; denial or invalid, stale, replayed, post-reload, wrong-origin, wrong-ID, wrong-version, or wrong-OAuth-transport input sends `AppCancelCeremony` to the active participant and requires fresh OAuth when retried. |
| LIBID-OAUTH-007 | The selected platform ceremony rejects the wrong transport, a nonempty disallowed transport, mixed transport, malformed encoding, duplicate authoritative fields, mixed success/error, wrong state/client/redirect/platform, and a Google ID Token at or after its signed `exp` before proving. Mutable X/GitHub proof lifetimes are tested at the Platform Verifier, not enforced by the browser. |
| LIBID-OAUTH-010 | Before credential-bearing network use, the active top-level prover requires `crossOriginIsolated`, `SharedArrayBuffer`, and its required prover workers. Missing isolation, popup-connection continuity, shared memory, or worker support clears inputs and aborts rather than selecting an iframe, auxiliary popup, single-threaded path, or weaker prover. |
| LIBID-OAUTH-011 | Before acceptance, authorization fields, code verifier, and OAuth return remain memory-only. Query/fragment and derived verifier cross only the bound popup connection and never enter storage, later URLs, telemetry, errors, or the public API. The authorization nonce stays client-side until accepted `OAuthProof`. Same-origin app scripts remain inside the trust boundary. |
| LIBID-OAUTH-012 | The prover sends one generic proof value. The live platform/version selects `validateProofMessage`, whose narrowed result is assembled with retained fields into discriminated `OAuthProof` and non-authoritative Identity. Literal and generic platform types preserve inference; dynamic values narrow by platform and version. New platform slices do not change CCDP, and neither prover nor client verifies the final proof. |
| LIBID-OAUTH-013 | After `AppRequestProof`, the prover returns one proof unchanged through the bound popup connection. The selected platform/version validator enforces the exact Google or X/GitHub proof shape and bounds; malformed, oversized, cross-platform, and cross-version values fail. Interruption before accepted `IdentityResult` requires fresh OAuth. |
| LIBID-OAUTH-014 | The live Ceremony accepts delivery only on its supplied authenticated popup connection, checks `OAuthProof`, derives Identity with the authenticated client, and resolves accepted. That status means OAuth-platform approval and local checks, not Ledger Verifier acceptance; wrong origin, cancellation, or replay returns no accepted result. |
| LIBID-OAUTH-015 | The GitHub token endpoint is synchronous and stateless, with no action, proof job, polling, result, progress, cancellation, or retry record; launch exposes no browser TLS bridge route. |
| LIBID-OAUTH-016 | The live Ceremony freezes the `CeremonyConfig`-selected client and redirect plus the nullable code verifier derived from the Authorization Digest and `authorizationNonce` by the normative PKCE construction. Accepted `AppRequestProof` carries the frozen client, redirect, and derived verifier but not `authorizationNonce`. The authorization request serializes OAuth `state` as exact `v<CCDPVersion>.<ceremonyId>`; no separate OAuth-state or PKCE-randomness value exists. |
| LIBID-OAUTH-017 | Navigate the application from allowed origin A to allowed origin B during OAuth. B cannot inherit A's popup connection or receive `CallbackDeliverParams`; only a connection authenticated for the exact live app origin and cleared OAuth state's `ceremonyId` can deliver the return. |
| LIBID-OAUTH-018 | A valid ceremony-bound OAuth-platform denial in `CallbackDeliverParams` makes the client resolve `proveUserIdentity()` with `{ status: 'denied' }` and send `AppCancelCeremony` through its popup connection without exchange, proving, or Job access. Whichever of Callback, Airlock, or Prover is active treats the cancellation identically and clears reachable input without closing or navigating the popup. The composition owns any resulting Job and popup transition. A failure after accepted OAuth rejects rather than resolving as denial. |
| LIBID-OAUTH-019 | An unknown ceremony or wrong callback version releases no callback value. Malformed, mismatched, unsupported-ceremony-version, and later internal-validation failures return no Identity; after connection selection, the Ceremony sends `AppCancelCeremony`. A valid success atomically consumes that Ceremony's one callback slot, waits for one `ProverReady`, sends one `AppRequestProof`, runs one attempt, and permits no second request; an early request, cancellation, or a racing terminal outcome changes no state or makes a late result inert. |
| LIBID-OAUTH-020 | An OAuth-platform callback whose local opener path is absent, severed, or rejected releases no OAuth bytes through it. Delivery may continue only through the optional popup fallback constructor supplied to both connection endpoints; when it is omitted or fails, the callback fails closed without delivery. A late local connection is inert. |
| LIBID-OAUTH-021 | `AppRequestProof` contains exactly platform ID/version, client ID, redirect URI, unchanged bounded `oauthReturn`, and nullable code verifier. The selected prover validator requires null for Google or the exact derived 43-character verifier for X/GitHub. Authorization nonce, digest, operation fields, and app origin never cross this boundary. |
| LIBID-OAUTH-022 | Every exact CCDP record matches its definition and creator-prefixed discriminator; `AbortCeremony` is the shared exception. Its same-named decoder exact-validates shape, fields, bounds, and discriminator and returns the received object unchanged. Each endpoint registers only its permitted inbound decoders. `PrefetchStarted` and `ProverReady` contain only their discriminator, `CallbackDeliverParams` only bounded `oauthReturn`, and proof delivery only `proof: unknown`; no CCDP message carries connection correlation. Exact direction, predecessor, cardinality, and terminal rules are enforced, so unknown fields, wrong direction/order—including `AppRequestProof` before `ProverReady`—replay, and post-terminal values change no ceremony state. |
| LIBID-OAUTH-023 | Callback-origin persistence contains no action, credential, token-exchange attestation, transcript, witness, code verifier, prover input, generated `OAuthProof`, verified result, or proof-delivery record. |
| LIBID-OAUTH-024 | Missing, canceled, wrong-ID, wrong-origin, unsupported-ceremony-version, and replayed live deliveries fail without returning Identity or reviving a Ceremony. |
| LIBID-OAUTH-025 | Destroy the application-side `Ceremony`, popup connection, isolated prover, or application document after proof generation but before `proveUserIdentity()` resolves. No proof can be recovered; a new ceremony requires fresh OAuth. |
| LIBID-OAUTH-026 | An initial `${ccdpOrigin}/ccdp/v1/prefetch#ceremonyId=<uuid>&platformId=<id>&ceremonyVersion=<uint>` fragment is cleared before subresources and runs the CCDP v1 Prefetch implementation. The top-level Prefetch accepts the popup connection, activates `/ccdp/v1/worker.js` containing popup continuity and prover prefetch/cache handlers, and reports fieldless `PrefetchStarted` after dispatching that profile's prefetch. A missing/malformed profile, document-load failure, unsupported CCDP path, or worker registration/activation failure rejects before OAuth; ordinary asset fetch failure follows the cold path. The client accepts `PrefetchStarted` only through the supplied connection, then calls `PopupConnection.navigateAway(platformAuthorizationUrl)` without disclosing that URL to the Prefetch carrier. No download timeout is required. |
| LIBID-OAUTH-027 | `CeremonyClient.new` receives exactly one caller-constructed `PopupConnection`; `proveUserIdentity()` receives no connection, popup, carrier, or launch option, opens no browsing context, runs the complete one-shot flow over that retained connection, and resolves with exactly one `IdentityResult`: accepted with a locally checked, non-authoritative Identity preview or denied. The popup package exposes only decoded registered messages; ceremony participants still enforce CCDP order and state. |

## OAuth Bridge and CCDP Distribution deployment

| ID | Assertion |
|---|---|
| KIT-001 | The OAuth Bridge exposes stateless `GET /api/v1/ceremony/config`, one configured callback path (default `/auth/callback`), and only the confidential routes required by enabled platforms. The independent CCDP Distribution exposes `GET /ccdp/v{CCDPVersion}/callback.js`, `GET /ccdp/v{CCDPVersion}/prefetch`, `GET /ccdp/v{CCDPVersion}/airlock`, `GET /ccdp/v{CCDPVersion}/prover`, `GET /ccdp/v{CCDPVersion}/worker.js`, and its immutable proving assets. Neither exposes an OAuth preparation route. |
| KIT-001A | `build:ccdp-artifacts:cf` materializes the validated artifact graph as one complete Cloudflare Pages directory containing exact CCDP route files, proving assets, inert `404.html`, and generated `_headers`, with no custom manifest or server runtime. The target removes Pages' default wildcard CORS from every profile except cross-origin Callback and rejects overlapping rules or Pages header, file-size, and file-count limits. Unknown paths and versions return the inert 404 rather than an SPA fallback. `wrangler pages dev` serves the same output locally and `wrangler pages deploy` publishes it without source resolution, archive handling, request-time compilation, templating, or module execution. |
| KIT-002 | Selecting GitHub requires the OAuth bridge to implement the token endpoint fixed by `OAUTH_BRIDGE.md` and fails when its client secret, server-side notary configuration, or route is absent. The ceremony package contributes only the GitHub version's prover-side browser request/response codecs and validation. |
| KIT-003 | Google or X Proxy requires no OAuth Bridge ceremony state or confidential exchange route beyond public `CeremonyConfig` and callback-shell hosting; the CCDP Distribution remains independent and no browser-MPC bridge route exists. |
| KIT-004 | Deployment `allowedAppOrigins` is a nonempty, duplicate-free bridge-controlled list of canonical HTTPS origins with no protocol maximum. A duplicate or invalid member fails deployment; each listed origin can fetch configuration and complete the same callback flow, while an unlisted origin fails. The list is not part of public `CeremonyConfig`. |
| KIT-005 | Only the application-scoped client fetches `CeremonyConfig`, and the request requires its browser `Origin`. The OAuth Bridge configuration drives that CORS decision and the canonical `allowedAppOrigins` embedded into the request-invariant Callback shell. It also publishes and embeds one canonical CCDP origin. CCDP [resources](CCDP.md#documents-and-routes) perform no configuration request. Request `Origin`, `Referer`, query, fragment, and platform code cannot alter those deployment inputs. |
| KIT-006 | The registered redirect URI is canonical HTTPS on the configured OAuth Bridge origin with no credentials/query/fragment; the explicit loopback profile is the only HTTP exception. Its developer-configurable path defaults to `/auth/callback`. The request-invariant shell bounds and clears the raw query/fragment, extracts exactly one `v<CCDPVersion>.<ceremonyId>` state, checks that version against its closed list, resolves its optional input override against the current default, then imports and invokes `{ccdpOrigin}/ccdp/v{CCDPVersion}/callback.js` in the same document. It performs no HTTP redirect or second document navigation. |
| KIT-007 | `ceremonyId` is a fresh lowercase UUIDv4 string serialized as the suffix of `v<CCDPVersion>.<ceremonyId>` OAuth `state` and reused as the popup connection ID; it is never a proof nonce or chain authorization. The Callback shell uses only the version prefix to select `{ccdpOrigin}/ccdp/v{CCDPVersion}/callback.js` before package loading. `authorizationNonce` is a fresh 32-byte random value and also supplies the PKCE derivation input. Only the derived code verifier crosses the prover boundary, and the nonce is published only in the accepted `OAuthProof` after the token exchange completes. No separate PKCE-randomness value exists. |
| KIT-008 | Reusing a live ceremony ID, or supplying malformed, padded, derived, wrong-version, wrong-variant, or wrong-case values, fails without weakening ceremony state or popup-connection checks. |
| KIT-009 | The OAuth Bridge callback shell and the CCDP Distribution's [resources](CCDP.md#documents-and-routes) are independently request-invariant at their respective deployments; no request field changes CSP, embedded code, script URL, origin, platform, or document role. |
| KIT-010 | The OAuth Bridge callback shell contains only one empty mount point and one CSP-hashed inline module bootstrap. Before any other rendering, storage, error reporting, or network use, the bootstrap bounds and copies query/fragment, clears both, selects one closed CCDP version, resolves its optional opaque input override against the default, deeply freezes the location and inputs, imports `{ccdpOrigin}/ccdp/v{CCDPVersion}/callback.js`, and invokes it once. The implementation exact-validates deployment-owned application origins and CCDP origin. Malformed or unsupported input loads no implementation and renders only fixed text after clearing. |
| KIT-011 | X/GitHub callback queries are absent from server/proxy logs, traces, analytics, and error reports. |
| KIT-012 | The Callback HTTP response and selected implementation perform no token exchange or server mutation. After URL clearing, popup-connection acceptance, ceremony-ID claim, and transfer of the closed GitHub platform-return variant, only the isolated GitHub Prover calls the fixed stateless token route. |
| KIT-013 | `/api/v1/ceremony/github-token` is the GitHub v1 OAuth Bridge endpoint. Its preflight and POST exact-check the configured CCDP origin and return noncredentialed CORS. It accepts exact JSON `{ code, codeVerifier }`, uses bridge-owned secrets/settings, and returns exact JSON `{ accessToken, tokenAttestation: { attestedData, signature }, bearerOpening }`; byte strings are canonical base64url and the opening is 16 bytes. Wrong origin/method/query/media type, malformed or noncanonical fields, redirects, and correlation failure reject before dependent `/user` work. |
| KIT-014 | Token-exchange mutation, timeout, restart, duplicate request, and response loss retain no server state. The valid bounded response remains browser memory-only; loss before `proveUserIdentity()` returns requires fresh OAuth. |
| KIT-015 | No API input, request, user control, retry, or browser probe selects browser-MPC transport; every launch X and GitHub ceremony uses the deployed Proxy transport. The notary itself opens the fixed platform socket, and no application, request, callback configuration, or prover message supplies its proxy, socket, destination, or routing. |
| KIT-016 | The CCDP Distribution serves cross-origin-loadable `/ccdp/v{CCDPVersion}/callback.js`, top-level `/ccdp/v{CCDPVersion}/prefetch`, non-isolated `/ccdp/v{CCDPVersion}/airlock`, isolated `/ccdp/v{CCDPVersion}/prover`, shared `/ccdp/v{CCDPVersion}/worker.js`, and every browser-fetched proving asset from the configured CCDP origin, independently of any OAuth Bridge. Two Bridges with disjoint application allowlists can select the same unchanged Distribution; neither is registered by it. Proving-asset paths are implementation-private and immutable. Prefetch, Airlock, and Prover execute their entry code directly; Airlock and Prover resolve the same Worker registration so the popup package can preserve their carrier across isolation, while the Worker also owns ceremony prefetch/cache and Callback installs no Worker. Replacing either deployment is an independent code-supply-chain trust choice. |
| KIT-017 | An unsupported CCDP implementation path, broad script/worker/frame source, connect source beyond the Prover's defined HTTPS Bridge class and build-pinned network origins, request-derived CSP, or asset redirect is rejected as a production deployment. |
| KIT-018 | OAuth Bridge Callback pages and CCDP Distribution pages are each served from a dedicated cookie-free origin with no unrelated same-origin API in the reference deployments; their origins may differ. |
| KIT-020 | The ceremony requires no canonical orchestration service, preparation endpoint, status server, action ledger, or proof-recovery service. Its network surface is the OAuth Bridge's public `CeremonyConfig`, callback shell, and fixed confidential platform routes, plus the independent CCDP Distribution's [resources](CCDP.md#documents-and-routes) and proving assets. |

## Browser isolation and response policy

| ID | Assertion |
|---|---|
| CSP-001 | The configured callback path (default `/auth/callback`) serves one top-level, non-isolated, no-store/no-referrer, non-frameable OAuth-return shell and does not itself sever a surviving application opener. The shell clears the return and imports only `{ccdpOrigin}/ccdp/v{CCDPVersion}/callback.js` selected by OAuth state. |
| CSP-002 | The top-level Prefetch and Airlock documents are non-isolated and non-frameable; the top-level Prover sends COOP same-origin and COEP require-corp. All three are no-cache/no-referrer on the CCDP origin. Prefetch installs the version-matched Worker; Airlock and Prover resolve that same registration and scope for popup continuity, and Prover uses it for ceremony prefetch/cache. Callback requests Airlock navigation only after delivering the OAuth return; Airlock accepts a fresh carrier before requesting Prover navigation. |
| CSP-003 | Every CCDP browser document uses `default-src 'none'`, `object-src 'none'`, `base-uri 'none'`, `form-action 'none'`, and no JavaScript `'unsafe-inline'`/`'unsafe-eval'`; an exact CSP hash permits only its deployment-generated inline code, while `style-src` permits only the package-published stylesheet hash. The Prover's `connect-src https:` for arbitrary validated OAuth Bridges is the only broad network scheme source. Mutated or additional markup/style is blocked. |
| CSP-004 | Route headers and asset paths are fixed deployment values; hostile query/fragment/origin strings cannot inject or expand CSP. |
| CSP-005 | The callback bootstrap records query/fragment transport, clears both URL components, and makes no storage/network/module action while raw callback bytes remain in the URL. |
| CSP-006 | `/ccdp/v{CCDPVersion}/prefetch` accepts only an empty query and `#ceremonyId=<uuid>&platformId=<id>&ceremonyVersion=<uint>`; `/ccdp/v{CCDPVersion}/airlock` and `/ccdp/v{CCDPVersion}/prover` accept only an empty query and `#ceremonyId=<uuid>`. The path selects CCDP version and document role. Each document clears its fragment and accepts the popup connection before CCDP traffic. Wrong-route, wrong-context, or unsupported-version use runs no protocol code. |
| CSP-007 | In Chromium, Firefox, WebKit, Pixel emulation, and iPhone WebKit emulation, the Callback bootstrap imports the exact `{ccdpOrigin}/ccdp/v{CCDPVersion}/callback.js` implementation once, while versioned Prefetch, Airlock, and Prover documents execute their embedded entry code without another entry-script request. Wrong context, import failure, wrong MIME, missing `nosniff`, CORS failure, or COEP incompatibility fails before protocol execution. |
| CSP-008 | Every proving Worker implementation is sourced from an immutable CCDP-origin URL; any toolchain-internal local `blob:` worker loads only those same-origin bytes, and no cross-origin worker bridge exists. |
| CSP-009 | Spawner workers permit only required `blob:`/worker/connect sources; leaf workers use `worker-src 'none'`; every worker has its own CSP. |
| CSP-010 | Real nested bb.js and TLSNotary workers and WASM load only from immutable CCDP-origin locations selected by the build. |
| CSP-011 | The same byte-identical Prover response handles two arbitrary canonical HTTPS OAuth Bridge origins without per-Bridge Distribution configuration. The GitHub leaf derives its fixed token route from the origin of the Ceremony Client's frozen `redirectUri` in `AppRequestProof` and exact-validates it before use; no second endpoint field exists. CSP uses `connect-src https:` for Bridge HTTP traffic and the build-pinned exact Notary WebSocket origin; script, worker, and asset sources remain closed. Requests use noncredentialed CORS. A compromised Prover implementation can use every network class admitted by the shared response, so CSP is not per-Bridge compartmentalization. |
| CSP-013 | Prefetch uses the top-level ceremony popup on the configured CCDP origin and sends `PrefetchStarted` after connection acceptance, Worker activation, and startup dispatch. The Application then privately navigates that popup away to OAuth. After OAuth, Callback sends `CallbackDeliverParams` and navigates to `${ccdpOrigin}/ccdp/v1/airlock#ceremonyId=<uuid>`. Airlock accepts a fresh carrier, preserves it through the version-matched Worker, and navigates to same-origin `${ccdpOrigin}/ccdp/v1/prover#ceremonyId=<uuid>`. Prover claims it and sends `ProverReady`; only then may the Application send `AppRequestProof`. No OAuth return enters any CCDP URL or Worker state, and no iframe, second window, button, user-agent selection, or second Prover exists. |
| CSP-014 | Airlock establishes a fresh authenticated popup carrier because a MessagePort cannot cross from the OAuth Bridge origin. It uses the opener when response policy preserves it and the configured fallback otherwise, then preserves that carrier across the same-origin isolated-Prover navigation. Prover claims the carrier before CCDP handling. Cleared ceremony-ID fragments are processed first; later messages omit the ID, and no credential, proof input, or proof bytes enter either URL. |
| CSP-015 | Prefetch, Airlock, Prover, and Worker resources use exact MIME, `nosniff`, isolation-compatible policy, `Cache-Control: no-cache`, and ETags so one versioned path may receive compatible updates. CCDP-origin implementation-private content-addressed chunks, circuits, notarization assets, toolchain WASM, and CRS bodies use long-lived immutable caching; one immutable URL is never reused for changed bytes or execution-relevant metadata. |
| CSP-016 | Every launch profile runs its exact multithreaded real-prover configuration in the promoted top-level prover. It checks isolation and shared memory before credential use; failure sends terminal `AbortCeremony` when the port is available, clears inputs, and never selects an iframe, single-threaded, unisolated, or auxiliary-window fallback. Missing required workers or nested-worker startup behaves identically. |
| CSP-017 | Scripted and real-anchor launch both reach `${ccdpOrigin}/ccdp/v1/prefetch#ceremonyId=<uuid>&platformId=<id>&ceremonyVersion=<uint>`. After clearing, the top-level Prefetch accepts any valid browser-observed HTTPS Application origin while the Application exact-validates `ccdpOrigin`; it activates the version-matched Worker, requests only that profile, and sends fieldless `PrefetchStarted` without waiting for downloads. The Worker exact-validates the request and keeps fetch work alive with `event.waitUntil`. The Application then navigates away to the OAuth Platform without exposing that URL to the Prefetch peer. |
| CSP-018 | Prefetch, Airlock, Prover, and their Worker are same-origin and version-matched; every browser-fetched proving asset is also served from that CCDP origin and pinned by the build. Airlock and Prover are under the same Worker registration and scope. Prefetch resolves one closed code-pinned profile asset set; the Worker admits only immutable CCDP-origin assets and uses fixed bb.js CRS loaders/SRS size. Prover code runs only in the isolated Prover document. Unknown profiles, off-origin URLs, redirects, opaque/partial responses, mutable aliases, and input-supplied assets fail before caching. |
| CSP-019 | Across cold, partial, warm, concurrent, cross-platform, reload, service-worker update/termination, quota/fetch-failure, and unsupported-worker cases, the complete selected module/chunk/nested-worker/WASM/circuit/CRS graph joins ordinary or CRS single flights and unselected profiles fetch nothing. Install uses `skipWaiting`, activation uses `clients.claim`, same-origin prefetch credentials match native module/worker requests, and requests outside the pinned prefetch set use native fetch. New documents reconnect to durable caches; artifact failure follows the same cold path, while registration or activation failure is terminal rather than weakening isolation or worker count. |

## Browser lifecycle and progress

| ID | Assertion |
|---|---|
| LIBID-BROWSER-001 | `Ceremony.launchUrl` is the frozen `${ccdpOrigin}/ccdp/v{CCDPVersion}/prefetch` URL whose closed fragment contains only ceremony ID, platform ID, and selected platform ceremony version. The caller uses one unique non-reserved target for both `PopupWindow.open` and its real anchor, constructs the popup connection, and passes it to `CeremonyClient.new`; argument-free `proveUserIdentity()` navigates the retained connection to the same URL. |
| LIBID-BROWSER-002 | A rejected or closed supplied popup connection fails before OAuth navigation. The opener-independent-return test below covers OAuth-platform-severed callback behavior; generic popup launch and authentication cases remain in the popup package plan. |
| LIBID-BROWSER-003 | The ceremony reuses one caller-supplied logical popup connection through Prefetch, OAuth-platform Authorization, Callback, Airlock, and Prover. Each participating document registers only its permitted CCDP messages and none opens an iframe, second popup, or second Prover. |
| LIBID-BROWSER-004 | After OAuth delivery, Callback calls `PopupConnection.navigate` for the exact Airlock location. Airlock accepts a fresh carrier on the CCDP origin, then calls `PopupConnection.navigate` for the exact same-origin Prover location. The popup package keeps its transferable carrier in the shared Worker before Airlock replacement, and Prover claims that same logical connection before CCDP handling. Ceremony defines no Worker record, handoff deadline, carrier branch, or recovery path; those remain popup-package internals. |
| LIBID-BROWSER-005 | Ceremony uses `ceremonyId` as the popup connection ID, while every CCDP message omits it. Only the Application receives `CallbackDeliverParams`; Airlock and Prover receive no OAuth return. Prover sends fieldless `ProverReady` before the Application may send `AppRequestProof`, and concurrent ceremonies cannot mix protocol state. |
| LIBID-BROWSER-006 | The Ceremony client reports only `authorization`, `oauth-validation`, and `proof-generation`, entering the last immediately before it sends `AppRequestProof` after `ProverReady`. Platform prover leaves report only their bounded code, label, status, and progress and cannot select or infer a common stage. `ProverNotifyEvent` carries only that platform step and a finite nonnegative `performance.timeOrigin + performance.now()` timestamp through the popup connection; the client accepts it only during its locally known `proof-generation` stage, preserves the fields, and publishes `CeremonyEvent`. It rejects unauthenticated or illegal transitions. Popup connection activity creates no common stage; no composition-owned preparation, confirmation, submission, or finality stage enters either event. |
| LIBID-BROWSER-007 | Google, X, and GitHub send post-isolation progress directly from the visible prover to the app through the popup connection with no callback or ceremony-data relay, and each enabled platform/version emits its exact version-owned diagnostic-span catalog and lifecycle. The prover remains visibly authoritative if the app misses advisory progress. |
| LIBID-BROWSER-008 | One live ceremony connection serializes progress messages. Duplicate, missed, or artificially reordered delivery affects UX only and cannot authorize, cancel, alter, or complete a ceremony. Connection loss provides no ceremony recovery. |
| LIBID-BROWSER-009 | Popup closure is neither success, denial, nor cancellation. A lost live ceremony returns no Identity and requires fresh authorization; popup observation mechanics belong to the popup package and downstream composition state is outside this package. |
| LIBID-BROWSER-010 | In cold and verified-warm profiles, the complete request direction of X's first notarized token session reaches X before the authorization-code deadline. Delaying completion of that request direction past the deadline abandons the ceremony; response receipt, the identity session, proving, and delivery may complete later. |
| LIBID-BROWSER-011 | Real-device tests cover each platform app installed/absent, authentication in app/browser, approval/denial, suspension, ignored close, and same-tab/openerless return. |
| LIBID-BROWSER-012 | Openerless return, including an OAuth-platform COOP policy which severs the retained opener, can continue only when both popup connection endpoints receive corresponding optional fallback constructors, without releasing callback parameters through window messaging. An omitted, mismatched, or failed fallback and a new-profile return fail closed without claiming cross-profile recovery. |
| LIBID-BROWSER-013 | Race `ceremony.cancel()` against live Identity delivery in both orders under the popup package's preferred and fallback carriers. Cancellation sends parameterless `AppCancelCeremony` through the connection. The active Callback, Airlock, or Prover clears reachable input; Prover also stops workers. No participant acknowledges cancellation or uses a platform-specific cancel path. Ceremony cleanup never calls `connection.close()` or `connection.navigate()`; after cancellation, denial, proof delivery, or abort, the application composition may retain, navigate, or close it. A cancellation which wins returns no Identity, and a late result cannot revive the Ceremony. |
| LIBID-BROWSER-014 | Two concurrent ceremonies use distinct target names, `PopupWindow` instances, popup connections, and ceremony state; either ceremony's OAuth-platform/prover navigation or CCDP traffic cannot replace or complete the other. |
| LIBID-BROWSER-015 | After OAuth delivery, Callback automatically navigates the existing popup to Airlock; Airlock accepts a fresh carrier and automatically preserves it into the isolated Prover. Ceremony opens no delayed window and requires no second activation or **Continue proving** action. |
| LIBID-BROWSER-016 | Proving stays in the foreground ceremony popup after its top-level browsing context becomes the isolated prover. No persistent prover runs under the original app. On mobile, background or suspend the application tab while only the popup is visible: active proving continues there, application-side delivery may wait for scheduling, and resumed delivery preserves order and outcome without selecting another carrier. |
| LIBID-BROWSER-017 | Every package-rendered Callback, Airlock, and active-Prover view contains the inline libID logo. During active proving the top-level Prover shows one accessible milestone-progress bar and package-owned status label; Callback and Airlock render only fixed transition or failure views. |
| LIBID-BROWSER-018 | Closing consent without a returned OAuth error emits no denial or terminal result; a valid ceremony-bound OAuth-platform denial resolves immediately as `{ status: 'denied' }`. `cancel()` opens no auxiliary context and best-effort stops reachable live work. The Callback implementation owns no GitHub server request; a remote stateless request may finish but no response is used. |
| LIBID-BROWSER-019 | Real-device qualification separately covers iOS memory pressure, suspension/eviction, browser chrome, popup blocking defaults, native OAuth-platform handoff, and background scheduling; Playwright WebKit/iPhone emulation cannot satisfy these gates. |
| LIBID-BROWSER-020 | **Restart authorization** never opens a clean callback or proof-recovery context; when the live ceremony is gone the caller creates a fresh `Ceremony` and uses its initial real-anchor/synchronous-open launch with fresh OAuth. |
| LIBID-BROWSER-021 | Reload destroys the in-memory ceremony map and every live Ceremony. No callback, progress record, stale popup handle, or prior ceremony ID is presented as resumable or can recover proof delivery. |
| LIBID-BROWSER-022 | Destroy and recreate the application-side `Ceremony`, popup connection, Callback, Airlock, and top-level Prover at each applicable point in the live flow. No advisory progress, stale connection, or recreated context resumes OAuth, proving, or proof delivery. |
| LIBID-BROWSER-024 | Before the first prover event, the active prover UI shows **Preparing proof** with an empty active shimmer. Each valid event replaces the visible status with its bounded label and smoothly transitions only to its monotonic reported target; a repeating active-edge shimmer continues while the target is unchanged. Regressing, nonfinite, out-of-range, oversized-label, control-character, caller-derived, or out-of-order progress changes no UI state. It makes no elapsed-time interpolation or ETA claim, never moves backwards, and reaches 100% only on `ProverDeliverProof`. |
| LIBID-BROWSER-025 | After 15 seconds of active proving, fake-timer and real Vanadium tests show a nonblocking **Still proving** notice with optional per-site JavaScript-JIT guidance. The notice does not diagnose the cause, require JIT, user-agent sniff, reload, cancel, emit CCDP or `CeremonyEvent`, alter a timeout, or affect proof authority. Terminal cleanup removes the timer. |
| LIBID-BROWSER-028 | With an opener-independent fallback supplied, Callback sends exactly one bounded `CallbackDeliverParams` before navigating to Airlock. Airlock selects a fresh carrier, preserves it into Prover, and forwards no OAuth value. Prover sends exactly one `ProverReady`; only the Application validates the return and later sends `AppRequestProof`. CCDP neither participates in fallback signaling nor defines another OAuth-delivery shape. |

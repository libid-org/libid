# `@libid/ceremony` test plan

This plan tests the `@libid/ceremony` package architecture in
[ARCHITECTURE.md](ARCHITECTURE.md) and its browser protocol in
[CCDP.md](CCDP.md), its prover subsystem in [PROVER.md](PROVER.md), and the
integrating-server contract in [SERVER.md](SERVER.md).
Normative proof and platform behavior come from the
[common ceremony rules](../../../specs/ceremony-common.md) and
[identity-platform ceremonies](../../../specs/platform-ceremonies.md).

Every row is one stable requirement and may require multiple positive and
negative cases. Tests **must cite its ID**. Existing IDs retain their names;
never renumber or reuse one.

A row belongs here only when it exercises `@libid/ceremony`, an emitted browser
asset, or a server/browser contract declared by the package. Consumer-owned Job
transitions, wallet preparation or confirmation, transaction submission, and
post-ceremony recovery belong to their respective consumer test plans.

## Package and asset isolation

| ID | Assertion |
|---|---|
| LIBID-MOD-002 | The `@libid/ceremony/prover` entrypoint emits one dual-context `libid-ceremony-prover.js`: its Window branch exports only `startProver(fragment, assets, port?)` and owns selected-profile iframe prefetch, one active top-level prover, and the package-owned visible UI, while its Service Worker branch exports no protocol API and runs the `PortKeeper` handlers, ordinary immutable-asset Cache Storage single flights, and bb.js CRS-loader single flights into bb.js's IndexedDB cache. Its bundle contains no final-proof verifier, callback, React/UI framework, wallet key, connector, Registry submission, or wallet-policy code. |
| LIBID-MOD-003 | The `@libid/ceremony/callback` entrypoint emits `libid-ceremony-callback.js` with every normal callback view, the inline libID logo, and exact stylesheet text in one root artifact. Server HTML contains only its security bootstrap and empty mount point; no separate logo, stylesheet, UI template, theme, renderer, or application customization input exists. The bundle contains no proving implementation, React/UI framework, wallet key, connector, Registry submission, or wallet-policy code. |
| LIBID-MOD-011 | `@libid/ceremony/client` exact-validates one `CeremonyConfig` record, including each nonempty duplicate-free `ceremonyVersions` list, and fetches it once per client. Callback and prover never fetch it. |
| LIBID-MOD-012 | The initial callback loads `/api/v1/ceremony/prover`, and the prover artifact owns both selected-profile prefetch and proof execution. Prefetch exposes only `ProverPrefetchingAssets` to the application; no client prefetch API, separate prefetch module, artifact, route, mode, or separately versioned package exists. |
| LIBID-MOD-013 | `@libid/ceremony` contains no server handler or secret. The GitHub version's prover leaf implements only the bounded `TokenRequest` and `TokenResponse` codecs and validation fixed by SERVER.md for the server-provided token endpoint; ccdp, client, callback, prover, worker, platform, and browser exports build with server implementation unavailable. |
| LIBID-MOD-014 | The ceremony client's public operation input contains exactly a 32-byte `chainId` hash, enabled `platformId`, 32-byte `operationDomain` hash, and bounded opaque `transactionData`. It rejects unknown fields, malformed hash lengths, ceremony version, authorization nonce, authorization digest, OAuth client, redirect, proof-domain, Registry, or finalizer values. |
| LIBID-MOD-015 | For every `CeremonyClient.new`, the client exact-validates and copies the supplied chain and operation-domain hashes without deriving them, selects the numerically greatest `PlatformCeremonyVersion` present in both its closed local catalog and that platform's `CeremonyConfig` independent of list or object-key order, draws a fresh cryptographically secure 32-byte authorization nonce, reproduces the normative authorization digest and code verifier, and freezes every input and derived value before provider navigation; later caller mutation changes nothing. |
| LIBID-MOD-016 | `@libid/ceremony/ccdp` builds without platform, browser, network, storage, Job, wallet, or UI code. All participants share its message decoders and enforce their order; one concrete typed `CCDPTransport` dispatches messages, while each carrier authenticates both endpoints and adapts its native resource without changing `Message`. `ProverNotifyEvent` contains only type, bounded `PlatformStep`, and prover timestamp; the client alone adds one of the three common stages to `CeremonyEvent`. |
| LIBID-MOD-017 | One application-scoped `CeremonyClient` fetches and validates `CeremonyConfig`, excludes configured platforms missing from its closed implementation or sharing no ceremony version with it, and creates independent stateful ceremonies through `new(ceremonyId, input)`. Calling `new` for an excluded platform fails before launch. No package-global singleton, generic Host, mutable popup setter, Job-store dependency, Registry callback, or composition plugin exists. |
| LIBID-MOD-018 | A created `Ceremony` exposes only its fixed `CeremonyNavigation`, event subscription, `proveUserIdentity`, and best-effort `cancel`; authorization is not exposed as a separate instance property. `CeremonyNavigation` contains only the shared `/api/v1/ceremony/callback` `href` with a closed ceremony-ID/platform-ID/ceremony-version fragment and target. The caller owns synchronous creation of an `about:blank` popup and real-anchor fallback. `proveUserIdentity({ expectedPopup })` navigates and pins one non-null source, while omitted `expectedPopup` opens and navigates nothing and binds the matching `ProverPrefetchingAssets` source. No public provider URL, asset URL, nullable popup value, or mutable popup setter exists. |
| LIBID-MOD-019 | One closed client-safe catalog derives platform IDs, supported versions, and proof types from version validators without importing prover leaves. `validateProofMessage` performs the selected runtime check before narrowing; any TypeScript assertion stays behind it. The prover dispatch is exhaustive over the same keys. Adding a key requires its client, types, validator, and prover leaf; no second union/map, proof-version axis, or dynamic registration exists. |
| LIBID-MOD-020 | Each `CeremonyClient.enabledPlatforms` is an immutable intersection of `supportedPlatforms` and validated `CeremonyConfig` keys having at least one mutually supported ceremony version, in catalog order. It exposes no OAuth client, ceremony version, server configuration, or display metadata, and mutating a returned value cannot change ceremony admission. |
| LIBID-ASSET-002 | After URL clearing, callback and prover bootstraps import their configured immutable root URL exactly once and invoke its exported entrypoint. Import failure, wrong origin, wrong media type, or COEP incompatibility fails before entrypoint execution. |
| LIBID-ASSET-003 | The prover build pins its Noir and Aztec-distributed bb.js JavaScript, workers, WASM, and CRS locations; whether JavaScript is one root file or immutable companion chunks does not change that authority. `/api/v1/ceremony/prover` embeds only the global libID-built `tlsn_wasm.js` URL, per-profile libID circuit URLs, and the deployment's common Notary Service address. The prover derives the same-directory `tlsn_wasm_bg.wasm` URL. Ceremony, opener, query, callback, message, and application inputs cannot replace a dependency, asset URL, or Notary Service endpoint. |
| LIBID-ASSET-004 | A `PlatformCeremonyVersion` owns one platform's authorization-digest construction, OAuth grammar, proof construction, proof type and validator, and final `OAuthProof` assembly; the ceremony client selects the numerically greatest locally supported version advertised by the platform configuration and freezes it in the live Ceremony, `AppRequestProof`, and accepted `OAuthProof`. `platforms/authorization` supplies shared digest and PKCE helpers but has no independent version or mandatory policy: each platform-version slice owns whether and how it uses them. Chain, Registry, and contract-verifier versions do not affect that selection, and distinct Consumers may accept the same output. There is no independent proof-version axis. |
| LIBID-ASSET-005 | Changing one platform ceremony version does not version another platform or `CCDPVersion`; application input cannot select it, and `CeremonyConfig` only constrains selection to its advertised set. |
| LIBID-ASSET-006 | Every live callback/prover page retains the modules it loaded; the client dispatches the ceremony's exact ceremony version and never resolves `latest` during a ceremony. |
| LIBID-ASSET-007 | Every live `Ceremony` and `AppRequestProof` pin the exact public `PlatformCeremonyVersion`; the client and prover artifacts retain every version they still accept during their compatibility window and never resolve `latest` after creation. The callback validates only the generic CCDP bootstrap and port transfer. An unsupported version cannot be consumed and requires a fresh ceremony. |
| LIBID-ASSET-008 | One deployment source generates matching `CeremonyConfig` and `ProverAssets`. The prover embeds one immutable notary module URL, common notary origin, and circuit URL per enabled platform/version; X/GitHub share URLs and cache entries. Profiles must match the closed catalog and reject malformed, conflicting, or insecure values before fetch/OAuth. Toolchain assets and SRS size remain code constants; launch does not runtime-hash configured assets. |
| LIBID-ASSET-009 | A real external worker graph passes with the outer blob bridge and every nested bb.js/TLSNotary worker; direct cross-origin `new Worker(url)` is rejected and no unisolated fallback runs. |
| LIBID-ASSET-010 | Rotate `CeremonyConfig` while a ceremony is live: the browser continues with the exact client ID, redirect URI, and ceremony version frozen at creation; a server exchange which no longer supports that flight fails closed, and a fresh `CeremonyClient` uses current configuration. Callback and prover perform no mid-ceremony configuration fetch. |
| LIBID-ASSET-011 | Every profile resolves the same code-pinned ACVM, Noir ABI, bb.js prover assets, and launch-wide 2^18-point BN254 SRS plus fixed Grumpkin data. X and GitHub additionally select the same configured notarization client and `bearer-link` circuit; Google selects its configured `oidc_google` circuit. The first platform starts the fixed SRS flight set, and every later platform reuses it without a size upgrade or refetch. Only missing profile-specific assets are fetched. |
| LIBID-ASSET-012 | A cleared `/api/v1/ceremony/prover#prefetch(ceremonyId, platformId, ceremonyVersion)` bootstrap causes the Window branch and service worker to fetch only the exact selected ordinary-artifact profile and start the fixed launch bb.js CRS loaders. Importing bb.js alone fetches no CRS. The fragment cannot supply an asset URL or SRS size. Profiles for other enabled platforms remain unfetched; a later `AppRequestProof` selects the same profile and cannot widen it. |
| LIBID-ASSET-013 | Circuit-release conformance records the pinned EVM circuit sizes and actual SRS floors: `bearer_link` 42,006 fails at its mathematical 2^16 ceiling because bb.js 5.2 requires a positive multiple of its 4 MiB compressed-input chunk and succeeds at 2^17; `oidc_google` 179,443 succeeds at 2^18 and fails at 2^17. The checked-in launch-wide SRS constant is 2^18 and every profile proves under it. Browser user agent and deployment data cannot alter it. A changed circuit or bb.js requirement exceeding that constant fails the build. Splitting the fetch size later requires performance evidence, not a platform ceremony version change while proof semantics remain identical. |

## Platform proving

| ID | Assertion |
|---|---|
| LIBID-PROVER-001 | Every enabled profile maps in closed code to one circuit release artifact and compatible Noir/ACVM and bb.js versions, while the package supplies the same 2^18-point BN254 SRS and fixed Grumpkin data to all of them. A real browser UltraHonk proof for every profile generated with that SRS verifies under the release's key. Changing circuit bytes, ABI, public-input order, proof system, or bb.js compatibility without the required platform ceremony version update fails; changing the shared SRS fetch policy alone does not require one while proof semantics remain identical. No application, callback message, user agent, request value, or Consumer contract selects any of them. |
| LIBID-PROVER-002 | Google v1 emits its defined spans, strictly parses the ID Token/JWK, builds the private witness, and delivers bounded `GoogleProofV1` semantic fields without attestation or flattened inputs. The client validates shape/audience and derives the preview without verifying the proof. The pure adapter packs the digest and named fields into the exact 56 verifier inputs; ordering, length, packing, and one-byte mutations are vector-tested. |
| LIBID-PROVER-003 | X v1 emits its defined spans, notarizes token before identity, validates the bounded printable bearer, commits it with independent 16-byte blinders, reveals only the prescribed ranges, and proves `bearer-link`. It delivers one bounded Honk proof plus byte-exact token and identity attestations in order, with no sidecar or public-input array; commitments reconstructed from those attestations verify the proof. |
| LIBID-PROVER-004 | GitHub v1 emits its defined spans and exact-validates the server token response, attestation correlation, request bindings, layout, live fields, and bearer opening before `/user`; failure discards it. It then notarizes `/user`, proves shared `bearer-link`, and delivers one bounded Honk proof plus ordered byte-exact attestations. Local notary-signature verification is optional; downstream verification is authoritative. |
| LIBID-PROVER-005 | The X and GitHub profiles point to the same immutable `bearer-link` circuit URL and resolve the same global notarization-client module/WASM pair and code-pinned prover toolchain. Cross-platform prefetch reuses those cache entries; platform-specific OAuth, server exchange, transcript construction, parsing, and progress remain in their respective closed modules and cannot alter the circuit ABI. |
| LIBID-PROVER-006 | The selected notary release supplies sibling `tlsn_wasm.js` and `tlsn_wasm_bg.wasm` files built from the same notary release, including `set_progress_callback`, with the worker bootstrap embedded in the module. X and GitHub use that pair as independent immutable responses; Google neither fetches nor initializes it. A missing release member fails before a notarized request. |
| LIBID-PROVER-007 | Every successful run emits one `started` and one `completed` for every diagnostic span, including cache hits. Every span started before a failure emits exactly one terminal event, while later spans are absent. Parent spans contain their children, documented dependencies hold, and otherwise-overlapping spans need not form a total order. X proves that `identity-session` starts only after token notarization returns the bearer. Prover timestamps permit per-span duration diagnostics without exposing URLs, credentials, witnesses, proofs, or raw errors. |
| LIBID-PROVER-008 | Only X token, X identity, and GitHub identity call the internal notarization adapter. Callers supply an exact request and deterministic reveal selector; the adapter enforces 4-KiB sent/32-KiB received ceilings, derives the fixed Proxy WebSocket, and accepts one bounded final attestation frame after TLSNotary releases the channel. It rejects malformed framing, truncation, overflow, duplicate output, redirects, and transcript-limit overflow. Only the required bearer/opening/attestation survives each call. |
| LIBID-PROVER-009 | Before the notarization wrapper returns, every raw TLSNotary commitment and opening maps exactly once by direction, range, and value to the read-only decoding of the original signed attested-data bytes. For each X and GitHub token/identity pair, the uniquely framed bearer commitments equal `SHA256(accessToken || bearerOpening)` and their matching TLSNotary hashes, and the two openings are independently generated exact 16-byte values. Wrong direction, shifted range, changed hash, changed opening, duplicate or missing commitment, malformed layout, or correlation against re-encoded rather than original bytes fails. |
| LIBID-PROVER-010 | The read-only decoder reproduces every field and the pinned digest of the upstream `libid-rs` fixture referenced by NOTARIZATION.md. Valid one-byte mutations change the field/digest; malformed mutations, truncation, trailing or reordered bytes, oversized counts, overflow, invalid ranges, and alternate encodings fail. Received signed bytes are never re-encoded. |
| LIBID-PROVER-011 | Every platform/version progress catalog assigns a bounded package-owned display label to each code and nonnegative weights to leaf spans only. Parent weight is zero and leaf weights form one positive total. For every valid serial and parallel completion order, emitted progress equals `0.95 * completedWeight / totalWeight`, remains finite and monotonic in `[0, 1)`, does not advance on `started`, and never double-counts nested spans. A cache hit follows the same events; failure invents no later progress; `ProverDeliverProof` alone renders completion. Labels and weights contain no caller, credential, identity, URL, proof, witness, or raw error data. |

## Ceremony client and Ceremony Cross-Document Protocol (CCDP)

| ID | Assertion |
|---|---|
| LIBID-OAUTH-001 | The application-scoped client fetches and exact-validates server-owned `CeremonyConfig` once; unknown record fields, missing or wrongly typed values, noncanonical, empty, or out-of-range client, redirect, known-platform, or ceremony-version values, and duplicate ceremony-version entries fail before navigation. `allowedAppOrigins` is rejected as an unknown public field. An unknown platform entry is ignored; an unknown but valid ceremony version is ignored when another advertised version is locally supported; a platform with no local implementation or common version is unavailable. Callback and prover do not fetch configuration. |
| LIBID-OAUTH-002 | The frontend configuration request requires an exact browser `Origin` and matching noncredentialed CORS response. The initial callback sends `ProverPrefetchingAssets(ccdpVersion, ceremonyId, platformId, ceremonyVersion)` without authenticating its opener. Before OAuth the application transport starts its MessagePort connection without sending anything. After OAuth, a callback with a usable opener sends `MessagePortHandshake(applicationVersion, null)` without an ID or transferable; the pending application operation accepts it only from the retained popup source and configured callback origin, then returns the same record with the live ceremony ID and exactly one fresh `MessagePort`. Ceremony code supplies `CCDPVersion` as the application version. The callback accepts that response only from its exact opener and a server-allowed browser-stamped app origin and exact-matches the version and ID to its binding and cleared OAuth state before releasing `CallbackDeliverParams` through the concrete transport. No payload origin, request `Origin`/`Referer`, or ceremony-server record authenticates the MessagePort carrier. |
| LIBID-OAUTH-003 | The ceremony caller may supply only the selected Chain Profile's exact 32-byte chain-ID hash, platform ID, exact 32-byte operation-domain hash, and transaction data. It cannot substitute server, client, redirect, ceremony version, authorization nonce/digest, notary, asset URL, or proof destination. |
| LIBID-OAUTH-005 | Callback bootstrap bounds and copies both URL components and clears query/fragment before parsing, storage, rendering, errors, module fetch, or any connection, including malformed and oversized input. |
| LIBID-OAUTH-006 | The callback extracts one valid OAuth state and releases the unchanged `oauthReturn` without repeating the ID only after MessagePort authentication or RTC binding. The client claims the bound live Ceremony and replies once through that transport with `AppRequestProof` or `AppCancelCeremony`; invalid, stale, replayed, post-reload, wrong-origin, wrong-ID, wrong-version, or wrong-transport input restarts OAuth. |
| LIBID-OAUTH-007 | The selected platform ceremony rejects the wrong transport, a nonempty disallowed transport, mixed transport, malformed encoding, duplicate authoritative fields, mixed success/error, wrong state/client/redirect/platform, and a Google ID Token at or after its signed `exp` before proving. Mutable X/GitHub proof lifetimes are tested at the Platform Verifier, not enforced by the browser. |
| LIBID-OAUTH-009 | Initial readiness and local callback authentication use the retained popup `WindowProxy` with exact target/browser-stamped origins and source equality. Events from another source are ignored. Before binding, any event from the bound source with the wrong origin, discriminator, shape, direction, application version, ID, or port count rejects the binding and closes every reachable port. Acceptance transfers exactly one port and retires the window listener; later window traffic is inert, while a selected-port `messageerror` closes that carrier. Every later application/prover message uses only the concrete `CCDPTransport`. Chromium, Firefox, and WebKit preserve ordering and queued messages while the Service Worker preserves the carrier port across immediate top-level navigation, without `BroadcastChannel`, another popup, or later opener reliance. |
| LIBID-OAUTH-010 | Before credential-bearing network use, the active top-level prover requires `crossOriginIsolated` and `SharedArrayBuffer`. Missing isolation after navigation, `PortKeeper` claim, worker support, or shared memory clears inputs and aborts rather than selecting an iframe, auxiliary popup, single-threaded path, or weaker prover. |
| LIBID-OAUTH-011 | Before acceptance, authorization fields, code verifier, and OAuth return remain memory-only. Query/fragment and derived verifier cross only the bound callback/app/prover transport and never enter storage, later URLs, telemetry, errors, or the public API. The authorization nonce stays client-side until accepted `OAuthProof`. Same-origin app scripts remain inside the trust boundary. |
| LIBID-OAUTH-012 | The prover sends one generic proof value. The live platform/version selects `validateProofMessage`, whose narrowed result is assembled with retained fields into discriminated `OAuthProof` and non-authoritative Identity. Literal and generic platform types preserve inference; dynamic values narrow by platform and version. New platform slices do not change CCDP, and neither prover nor client verifies the final proof. |
| LIBID-OAUTH-013 | After `AppRequestProof`, the prover returns one proof unchanged through the bound transport. The selected platform/version validator enforces the exact Google or X/GitHub proof shape and bounds; malformed, oversized, cross-platform, and cross-version values fail. Interruption before accepted `IdentityResult` requires fresh OAuth. |
| LIBID-OAUTH-014 | The live Ceremony accepts delivery only on its authenticated one-shot transport, checks `OAuthProof`, derives Identity with the authenticated client, and resolves accepted. That status means provider approval and local checks, not Consumer acceptance; wrong origin, cancellation, or replay returns no accepted result. |
| LIBID-OAUTH-015 | The GitHub token endpoint is synchronous and stateless, with no action, proof job, polling, result, progress, cancellation, or retry record; launch exposes no browser TLS bridge route. |
| LIBID-OAUTH-016 | The live Ceremony freezes the `CeremonyConfig`-selected client and redirect plus the nullable code verifier derived from the Authorization Digest and `authorizationNonce` by the normative PKCE construction. Accepted `AppRequestProof` carries the frozen client, redirect, and derived verifier but not `authorizationNonce`. The authorization request serializes its existing `ceremonyId` unchanged as OAuth `state`; no separate OAuth-state or PKCE-randomness value exists. |
| LIBID-OAUTH-017 | Navigate the opener from allowed origin A to allowed origin B during OAuth. B may receive `MessagePortHandshake(applicationVersion, null)` but receives no ceremony ID; a missing, guessed, replayed, wrong-version, or wrong-ID response releases no `CallbackDeliverParams`. A valid response from the retained source succeeds only when its browser-stamped origin is allowed and its application version and ID exact-match the current binding and cleared OAuth state. Messages from unrelated `WindowProxy` sources are ignored. |
| LIBID-OAUTH-018 | A valid ceremony-bound provider denial in `CallbackDeliverParams` makes the client resolve `proveUserIdentity()` with `{ status: 'denied' }` and send `AppCancelCeremony` through the concrete transport without exchange, proving, or Job access. The isolated prover treats every pre-proof cancellation identically: it clears queued input, attempts to close, and renders one fixed fallback message if closing fails. The composition owns any resulting Job transition. A failure after accepted OAuth rejects rather than resolving as denial. |
| LIBID-OAUTH-019 | An unknown ceremony or wrong callback version releases no callback value. A missing retained source sends no valid `MessagePortHandshake` response and may select WebRTC only through that ceremony's already-started signaling subscription. Malformed, mismatched, unsupported-ceremony-version, and later internal-validation failures return no Identity; after carrier selection, the client sends `AppCancelCeremony`. A valid success atomically claims its live ceremony-map entry, sends one `AppRequestProof`, runs one attempt, and permits no second request; cancellation or a racing terminal outcome makes a late result inert. |
| LIBID-OAUTH-020 | A provider callback with a missing or severed opener, or no valid `MessagePortHandshake` response by `CALLBACK_OPENER_TIMEOUT_MS`, releases no OAuth bytes locally. It queues one bounded `CallbackDeliverParams` on a port kept with purpose `rtc-bootstrap` and allows only that ceremony's already-started RTC signaling subscription to bind. A late local authentication is inert. |
| LIBID-OAUTH-021 | `AppRequestProof` contains exactly platform ID/version, client ID, redirect URI, unchanged bounded `oauthReturn`, and nullable code verifier. The selected prover validator requires null for Google or the exact derived 43-character verifier for X/GitHub. Authorization nonce, digest, operation fields, and app origin never cross this boundary. |
| LIBID-OAUTH-022 | Every exact CCDP record matches its definition and creator-prefixed discriminator; `AbortCeremony` is the shared exception. Its same-named `Decoder` exact-validates shape, fields, bounds, and discriminator and returns the received object unchanged. Each endpoint registers only its permitted inbound decoders; duplicate, unknown, and unregistered discriminators fail, and transport invokes the selected decoder exactly once for every inbound carrier value. Only initial `ProverPrefetchingAssets` carries ceremony ID and CCDP version so the CCDP handler can validate it before application transport binds the popup source; every later CCDP message omits them because concrete transport ownership supplies that binding. Private carrier, continuity, and RTC-signaling controls carry the caller-supplied application version and their required correlation fields, but transport assigns the version no application semantics. `CallbackDeliverParams` carries only bounded `oauthReturn`, and proof delivery only `proof: unknown`. Unknown fields, wrong direction, wrong order, wrong carrier controls, and replay change no state. Carrier controls enforce their own source, origin, role, and transferable rules. |
| LIBID-OAUTH-023 | Callback-origin persistence contains no action, credential, token-exchange attestation, transcript, witness, code verifier, prover input, generated `OAuthProof`, verified result, or proof-delivery record. |
| LIBID-OAUTH-024 | Missing, canceled, wrong-ID, wrong-origin, unsupported-ceremony-version, and replayed live deliveries fail without returning Identity or reviving a Ceremony. |
| LIBID-OAUTH-025 | Destroy the application-side `Ceremony`, ceremony popup window, transient `PortKeeper` entry, RTC peer/signaling subscription, isolated prover, or application document after proof generation but before `proveUserIdentity()` resolves. No proof can be recovered; a new ceremony requires fresh OAuth. |
| LIBID-OAUTH-026 | An initial `/api/v1/ceremony/callback` launch fragment contains exactly ceremony ID, platform ID, and ceremony version and is cleared before subresources. The callback embeds the same tuple in its exact `/api/v1/ceremony/prover#prefetch(...)` child, which clears it before importing its root module or using the network, activates the current prover worker, and forwards matching `ProverPrefetchingAssets` unchanged after dispatching that profile's prefetch. A missing/malformed profile, document-load failure, or worker registration/activation failure rejects before OAuth; ordinary asset fetch failure follows the cold path. The app exact-validates protocol version, callback origin, live profile/ID, and expected source when supplied; otherwise it binds the matching fallback source, then navigates it without replying. No download timeout is required. |
| LIBID-OAUTH-027 | `proveUserIdentity()` receives at most one caller-opened `expectedPopup`, opens no browsing context, runs the complete one-shot flow, and resolves with exactly one `IdentityResult`: accepted with a locally checked, non-authoritative Identity preview or denied. A supplied expected source is bound by the application transport constructor. Otherwise `bindPopup` admits only a value from the configured callback origin which the Ceremony Client exact-validates as this live ceremony's `ProverPrefetchingAssets`, then binds that value's browser-stamped source. Rejected candidates bind nothing. Transport exposes only messages accepted by a registered decoder and dispatches each to its typed handler; participants still enforce order and state. |
| LIBID-OAUTH-028 | Only ceremony transport constructs `PortKeeper`. Its async factory privately calls `claim(ceremonyId)` before new-carrier selection when an active registration is available: absence returns `null` and selects nothing, while a matching entry atomically returns the unchanged purpose and port. Before callback self-navigation to `/prover#ceremonyId`, `navigatePopup` privately calls `keep(ceremonyId, purpose, port)` and waits for worker ownership; its message event remains extended through claim or `CARRIER_CLAIM_TIMEOUT_MS = 5_000`. The prover awaits the same ceremony factory, which claims and exact-validates the continuation before returning. Application transport navigates its retained `WindowProxy` directly and constructs no keeper. Ceremony code supplies `CCDPVersion` as `applicationVersion`; no caller constructs a keeper or supplies continuity purpose, route, phase, or role to selection. Malformed or mismatched application version, internal origin, ID, purpose, or port count, duplicate ownership, worker loss, and post-terminal traffic fail closed without storage, another window, or a second prover. |

## Deployment and server configuration

| ID | Assertion |
|---|---|
| KIT-001 | The deployment exposes stateless `GET /api/v1/ceremony/config`, fixed `GET /api/v1/ceremony/callback`, a byte-identical alias at the configured `redirectUri` path (default `/auth/v1/callback`), fixed `GET /api/v1/ceremony/prover`, and only the confidential routes required by enabled platforms, including `POST /api/v1/ceremony/github-token` for GitHub; it exposes no OAuth preparation route. |
| KIT-002 | Selecting GitHub requires the integrating server to implement and mount the token endpoint fixed by SERVER.md and fails when its client secret, notary configuration, route, or immutable assets are absent. The ceremony package contributes only the GitHub version's prover-side browser request/response codecs and validation. |
| KIT-003 | Google or X Proxy requires no server ceremony state or confidential exchange route beyond public `CeremonyConfig` and fixed page/asset hosting; launch exposes no browser-MPC bridge route. |
| KIT-004 | Deployment `allowedAppOrigins` is a nonempty server-controlled list of canonical origins with no protocol maximum; membership uses `Set` semantics, so permutations and duplicates behave identically, each listed origin can fetch configuration and complete the same callback flow, and an unlisted origin fails. The list is not part of public `CeremonyConfig`. |
| KIT-005 | Only the application-scoped client fetches `CeremonyConfig`, and the request requires its browser `Origin`. The same server configuration drives that CORS decision and the canonical `allowedAppOrigins` embedded into the byte-identical `/api/v1/ceremony/callback` and configured-alias responses; `startCallback(oauthReturn, allowedAppOrigins)` exact-validates the list and authorizes only browser-stamped `MessageEvent.origin`. Callback and prover perform no configuration request. Request `Origin`, `Referer`, query, fragment, and client messages cannot select or alter the embedded list. |
| KIT-006 | The registered redirect URI is canonical HTTPS on the configured server origin with no credentials/query/fragment; the explicit loopback profile is the only HTTP exception. Its developer-configurable path defaults to `/auth/v1/callback`. Both the default and a nondefault path directly serve bytes and security headers identical to `/api/v1/ceremony/callback` and never issue an HTTP redirect. |
| KIT-007 | `ceremonyId` is a fresh lowercase UUIDv4 string serialized unchanged as OAuth `state` and used once for MessagePort authentication or RTC rendezvous; it is never a proof nonce or chain authorization. `authorizationNonce` is a fresh 32-byte random value and also supplies the PKCE derivation input. Only the derived code verifier crosses the prover boundary, and the nonce is published only in the accepted `OAuthProof` after the token exchange completes. No separate PKCE-randomness value exists. |
| KIT-008 | Reusing a live ceremony ID, or supplying malformed, padded, derived, wrong-version, wrong-variant, or wrong-case values, fails without weakening state or transport checks. |
| KIT-009 | Callback/prover HTML and headers are byte-identical across requests; no request field changes CSP, script URL, origin, platform, or mode. |
| KIT-010 | The CSP-hashed inline callback bootstrap performs only capture, clearing, immutable-root import, and exported-entrypoint invocation; it never interprets identity-platform fields or accesses storage/network before clearing. |
| KIT-011 | X/GitHub callback queries are absent from server/proxy logs, traces, analytics, and error reports. |
| KIT-012 | The callback HTTP response and `libid-ceremony-callback.js` perform no token exchange or server mutation. After URL clearing, one-use carrier binding, ceremony-ID claim, and transfer of the closed GitHub provider-return variant, only the isolated GitHub prover calls the fixed stateless token route. |
| KIT-013 | `/api/v1/ceremony/github-token` is the GitHub v1 same-origin POST endpoint. It accepts exact JSON `{ code, codeVerifier }`, uses server-owned secrets/settings, and returns exact JSON `{ accessToken, tokenAttestation: { attestedData, signature }, bearerOpening }`; byte strings are canonical base64url and the opening is 16 bytes. Wrong origin/method/query/media type, malformed or noncanonical fields, redirects, and correlation failure reject before dependent `/user` work. |
| KIT-014 | Token-exchange mutation, timeout, restart, duplicate request, and response loss retain no server state. The valid bounded response remains browser memory-only; loss before `proveUserIdentity()` returns requires fresh OAuth. |
| KIT-015 | No API input, request, user control, retry, or browser probe selects browser-MPC transport; every launch X and GitHub ceremony uses the deployed Proxy transport. The notary itself opens the fixed platform socket, and no application, request, callback configuration, or prover message supplies its proxy, socket, destination, or routing. |
| KIT-016 | The deployment serves `libid-ceremony-prover.js` from the callback origin so the same URL can run as a module service worker; build-pinned external workers/WASM and configured libID assets still pass CORS and isolation loading. Replacing either source is a full code-supply-chain trust choice. |
| KIT-017 | A mutable root-module URL, broad script/connect source, request-derived CSP, or asset redirect is rejected as a production deployment. |
| KIT-018 | Callback/prover pages are served from a dedicated cookie-free origin with no unrelated same-origin API in the reference deployment. |
| KIT-020 | The ceremony requires no canonical orchestration service, preparation endpoint, continuation route, status server, server action ledger, or proof-recovery service. Its server surface is public `CeremonyConfig`, fixed browser documents/assets, and any fixed confidential platform routes. |

## Browser isolation and response policy

| ID | Assertion |
|---|---|
| CSP-001 | `/api/v1/ceremony/callback` and its direct configured alias (default `/auth/v1/callback`) serve the same top-level, non-isolated, no-store/no-referrer, non-frameable response and do not themselves sever a surviving application opener. |
| CSP-002 | The single prefetch/proving `/api/v1/ceremony/prover` response sends COOP same-origin, COEP require-corp, no-store/no-referrer, and same-origin-only framing. COOP severs the top-level popup opener only after the existing Service Worker acknowledges preservation of the selected carrier port. Prefetch and proving use the same registration and cache. |
| CSP-003 | Both documents use `default-src 'none'`, `object-src 'none'`, `base-uri 'none'`, `form-action 'none'`, no broad `https:`, and no JavaScript `'unsafe-inline'`/`'unsafe-eval'`; one exact CSP hash permits only the deployment-generated inline bootstrap, while `style-src` permits only the package-published hash of the root module's installed stylesheet text. Mutated or additional markup/style is blocked. |
| CSP-004 | Route headers and asset URLs are fixed deployment values; hostile query/fragment/origin strings cannot inject or expand CSP. |
| CSP-005 | The callback bootstrap records query/fragment transport, clears both URL components, and makes no storage/network/module action while raw callback bytes remain in the URL. |
| CSP-006 | `/api/v1/ceremony/prover` accepts only an empty query and either `prefetch(ceremonyId, platformId, ceremonyVersion)` in the initial child or a bare ceremony ID in the promoted top-level popup window. It clears the fragment before subresources, storage, package import, or network; the top-level bootstrap claims its port before importing the root. Wrong-context use imports nothing. |
| CSP-007 | In Chromium, Firefox, WebKit, Pixel emulation, and iPhone WebKit emulation, each executing bootstrap imports the exact immutable root-module URL once and reaches the exported entrypoint; a wrong-context bootstrap imports nothing. Import failure is terminal for that document, while a second root import, ambient-global handoff, wrong MIME, missing `nosniff`, CORS failure, or COEP incompatibility fails. |
| CSP-008 | Direct cross-origin Worker construction fails; the allowed local blob bootstrap imports only the immutable code-pinned worker module. |
| CSP-009 | Spawner workers permit only required `blob:`/worker/connect sources; leaf workers use `worker-src 'none'`; every worker has its own CSP. |
| CSP-010 | Real nested bb.js workers and WASM load only from their code-pinned locations, while TLSNotary workers and WASM load only from their immutable deployment locations; both pass through the recursive bridge. No same-origin mirror of either graph is required. |
| CSP-011 | The byte-identical prover response has one deployment-fixed `connect-src` containing the closed union of exact origins required by every enabled platform/version profile; no query, fragment, message, or selected profile changes it, and it contains no broad scheme or TLS bridge origin. The active platform's closed network catalog contains only its own exact configured or code-pinned URLs: X and GitHub use their pinned Proxy-notary origins, GitHub additionally uses its same-origin token route, and Google uses its pinned JWKS origin. Caller input cannot select another profile's CSP-permitted origin. The union is a ceiling for the trusted prover bundle, not browser-enforced cross-profile compartmentalization. |
| CSP-013 | Prefetch uses one same-origin prover iframe and reports readiness after the worker is active and startup dispatch settles. After OAuth, the non-isolated callback has the worker acknowledge preservation of its carrier port and replaces itself with `/api/v1/ceremony/prover#ceremonyId`. No post-callback iframe, second window, button, coordinator, user-agent selection, or second prover exists. |
| CSP-014 | The active isolated top-level prover has no opener reliance. Its cleared ceremony-ID fragment claims exactly one worker-preserved carrier port: either the application-bound MessagePort carrier endpoint or one locally queued OAuth return used to establish WebRTC. All later CCDP messages use the concrete transport and omit the ID. No role, credential, proof input, or proof bytes enter its URL. |
| CSP-015 | Every immutable prover, worker, and WASM response uses exact MIME, `nosniff`, CORS/CORP compatible with isolation, and `Cache-Control: public, max-age=31536000, immutable`; one URL is never reused for changed bytes or execution-relevant response metadata, and retained entries for the exact release serve the warm path. |
| CSP-016 | Every launch profile runs its exact multithreaded real-prover configuration in the promoted top-level prover. It checks isolation and shared memory before credential use; failure sends terminal `AbortCeremony` when the port is available, clears inputs, and never selects an iframe, single-threaded, unisolated, or auxiliary-window fallback. Missing required workers or nested-worker startup behaves identically. |
| CSP-017 | Scripted and real-anchor launch both reach `/api/v1/ceremony/callback#launch(ceremonyId, platformId, ceremonyVersion)`, which clears the fragment, loads its same-origin `/api/v1/ceremony/prover` child in the ceremony server's top-level partition, activates the current worker, and requests only that profile. It forwards matching `ProverPrefetchingAssets` without waiting for downloads. A receiving worker exact-validates the request and keeps fetch work alive with `event.waitUntil`. The application exact-validates protocol/profile versions, ID, source, and callback origin, retains the source, then immediately navigates it to the provider without replying. No application-hosted prefetch iframe, developer/application switch, prefetch route, mode flag, or separate artifact is a launch dependency. Cold and warm X paths meet its code deadline. |
| CSP-018 | The same-origin prover module runs as Window and scoped module worker. The Window validates embedded assets and resolves one closed profile; the worker admits only configured immutable assets and code-pinned toolchain resources and uses fixed bb.js CRS loaders/SRS size. Unknown profiles, origins, URLs, redirects, opaque/partial responses, mutable aliases, and input-supplied assets fail before caching. |
| CSP-019 | Across cold, partial, warm, concurrent, cross-platform, reload, service-worker update/termination, quota/fetch-failure, and unsupported-worker cases, the complete selected module/chunk/nested-worker/WASM/circuit/CRS graph joins ordinary or CRS single flights and unselected profiles fetch nothing. Install uses `skipWaiting`, activation uses `clients.claim`, same-origin prefetch credentials match native module/worker requests, and nonmanifest requests use native fetch. New documents reconnect to durable caches; artifact failure follows the same cold path, while registration, activation, or live `PortKeeper` failure is terminal rather than weakening isolation or worker count. |

## Browser lifecycle and progress

| ID | Assertion |
|---|---|
| LIBID-BROWSER-001 | `Ceremony.navigation` supplies one `/api/v1/ceremony/callback` `href` whose closed fragment contains only ceremony ID, platform ID, and selected ceremony version, plus the exact non-reserved `libid-${ceremonyId}` target. The caller synchronously opens `about:blank` with that target and neither `noopener` nor `noreferrer`; a null script result leaves anchor navigation untouched, while `proveUserIdentity({ expectedPopup })` navigates a successful handle to the same `href`. |
| LIBID-BROWSER-002 | Strictly isolated application pages, a missing initial popup `WindowProxy`, framing, synthetic/delayed launch, or an opaque sandbox origin fail before OAuth navigation. The openerless-return test below covers provider-severed callback behavior. |
| LIBID-BROWSER-003 | The ceremony reuses one popup through shared prefetch, identity-platform authorization, callback, and proving. The client retains either the non-null scripted-open handle or the exact browser-stamped source learned from matching initial `ProverPrefetchingAssets`; after OAuth, that source is authoritative for local MessagePort binding, while a severed opener can bind only through the same live ceremony's armed RTC subscription. The callback never opens a post-callback iframe, second popup, or second prover. |
| LIBID-BROWSER-004 | Chromium, Firefox, WebKit, Pixel emulation, and iPhone WebKit emulation preserve one purpose-tagged port through the worker while immediately navigating the same popup window to the top-level isolated prover. Delays below the shared 5-second claim deadline succeed; expiry at or after the deadline, worker termination, and OAuth-length or intentionally idle retention fail without recovery. The concrete transport owns and exact-validates the purpose: one value continues its bound MessagePort carrier, while the other transfers only the queued return before the final prover opens WebRTC. No user-agent sniffing, browser-specific deadline, or capability probe selects another placement. |
| LIBID-BROWSER-005 | The opener-independent prover transport claims one purpose-tagged port by its cleared ceremony ID. It either resumes an already bound MessagePort carrier or consumes one local `CallbackDeliverParams` and establishes WebRTC. Every CCDP message after prefetch readiness omits the ID; concurrent, stale, duplicate, or expired holders do not mix. |
| LIBID-BROWSER-006 | The Ceremony client reports only `authorization`, `oauth-validation`, and `proof-generation`, entering the last immediately before it sends `AppRequestProof`. Platform prover leaves report only their bounded code, label, status, and progress and cannot select or infer a common stage. `ProverNotifyEvent` carries only that platform step and prover-stamped time directly through the concrete transport; the client accepts it only during its locally known `proof-generation` stage, preserves the fields, and publishes `CeremonyEvent`. It rejects unauthenticated or illegal transitions. `PortKeeper` activity creates no common stage; no composition-owned preparation, confirmation, submission, or finality stage enters either event. |
| LIBID-BROWSER-007 | Google, X, and GitHub send post-isolation progress directly from the visible prover to the app through the concrete transport with no callback or ceremony-data relay, and each enabled platform/version emits its exact version-owned diagnostic-span catalog and lifecycle. The prover remains visibly authoritative if the app misses advisory progress. |
| LIBID-BROWSER-008 | One live ceremony transport serializes progress messages. Duplicate, missed, or artificially reordered delivery affects UX only and cannot authorize, cancel, alter, or complete a ceremony. Transport loss provides no ceremony recovery. |
| LIBID-BROWSER-009 | Popup closure is neither success, denial, nor cancellation. Both launch paths retain the popup handle after `ProverPrefetchingAssets`; `popup.closed` is only an advisory retry hint. Closure before real-anchor source binding has no observable handle. A lost live ceremony returns no Identity and requires fresh authorization; downstream consumer state is outside this package. |
| LIBID-BROWSER-010 | In cold and verified-warm profiles, the complete request direction of X's first notarized token session reaches X before the authorization-code deadline. Delaying completion of that request direction past the deadline abandons the ceremony; response receipt, the identity session, proving, and delivery may complete later. |
| LIBID-BROWSER-011 | Real-device tests cover each platform app installed/absent, authentication in app/browser, approval/denial, suspension, ignored close, and same-tab/openerless return. |
| LIBID-BROWSER-012 | Openerless return, including a provider COOP policy which severs the retained opener, selects RTC without releasing callback parameters through window messaging. A new-profile return has no matching live application ceremony or pending signaling subscription and fails closed as restart without claiming cross-profile recovery. |
| LIBID-BROWSER-013 | Race `ceremony.cancel()` against live Identity delivery in both orders under both carriers. Cancellation sends parameterless `AppCancelCeremony` through the concrete transport. The active prover stops workers/connections, clears inputs, and attempts `window.close()` without acknowledgement or a platform-specific cancel path; a remaining view renders the fixed fallback. A cancellation which wins returns no Identity, and a late result cannot revive the Ceremony. |
| LIBID-BROWSER-014 | Launch two concurrent ceremonies and require distinct target names, `WindowProxy` values, transport endpoints, worker holders, signaling entries, and RTC peers where used; either ceremony's provider/prover navigation or transport binding cannot replace or claim the other's resources. |
| LIBID-BROWSER-015 | After OAuth, the callback navigates its existing popup window automatically only after the worker acknowledges preservation of the selected carrier port. This requires no delayed `window.open`, real-anchor activation, or **Continue proving** action. |
| LIBID-BROWSER-016 | Proving stays in the foreground ceremony popup after its top-level browsing context becomes the isolated prover. No persistent prover runs under the original app. On mobile, background or suspend the application tab while only the popup is visible: active proving continues there, application-side delivery may wait for scheduling, and resumed delivery preserves order and outcome without selecting another carrier. |
| LIBID-BROWSER-017 | Every package-rendered callback and active-prover view contains the inline libID logo. During active proving the top-level prover shows one accessible milestone-progress bar and package-owned status label; the callback shell renders no competing coordinator or activation UI. |
| LIBID-BROWSER-018 | Closing consent without a returned OAuth error emits no denial or terminal result; a valid ceremony-bound provider denial resolves immediately as `{ status: 'denied' }`. `cancel()` opens no auxiliary context and best-effort stops reachable live work. `libid-ceremony-callback.js` owns no GitHub server request; a remote stateless request may finish but no response is used. |
| LIBID-BROWSER-019 | Real-device qualification separately covers iOS memory pressure, suspension/eviction, browser chrome, popup blocking defaults, native-provider handoff, and background scheduling; Playwright WebKit/iPhone emulation cannot satisfy these gates. |
| LIBID-BROWSER-020 | **Restart authorization** never opens a clean callback or proof-recovery context; when the live ceremony is gone the caller creates a fresh `Ceremony` and uses its initial real-anchor/synchronous-open launch with fresh OAuth. |
| LIBID-BROWSER-021 | Reload destroys the in-memory ceremony map and every live Ceremony. No callback, progress record, stale popup handle, or prior ceremony ID is presented as resumable or can recover proof delivery. |
| LIBID-BROWSER-022 | Destroy and recreate the application-side `Ceremony`, callback document, transient `PortKeeper` entry, RTC peer/signaling subscription, and top-level prover at each applicable point in the live flow. No advisory progress, stale popup handle, transport endpoint, or recreated context resumes OAuth, proving, or proof delivery. |
| LIBID-BROWSER-023 | Exercise featureful and featureless auxiliary launch across touch, pointer, desktop-mode, and tablet-with-trackpad cases. Browser promotion between popup and tab changes presentation only; no user-agent/input heuristic, integrator mode, or observed presentation changes security, storage, recovery, or proving behavior. |
| LIBID-BROWSER-024 | Before the first prover event, the active prover UI shows **Preparing proof** with an empty active shimmer. Each valid event replaces the visible status with its bounded label and smoothly transitions only to its monotonic reported target; a repeating active-edge shimmer continues while the target is unchanged. Regressing, nonfinite, out-of-range, oversized-label, control-character, caller-derived, or out-of-order progress changes no UI state. It makes no elapsed-time interpolation or ETA claim, never moves backwards, and reaches 100% only on `ProverDeliverProof`. |
| LIBID-BROWSER-025 | After 15 seconds of active proving, fake-timer and real Vanadium tests show a nonblocking **Still proving** notice with optional per-site JavaScript-JIT guidance. The notice does not diagnose the cause, require JIT, user-agent sniff, reload, cancel, emit CCDP or `CeremonyEvent`, alter a timeout, or affect proof authority. Terminal cleanup removes the timer. |
| LIBID-BROWSER-026 | The application transport starts one bounded, idle RTC signaling subscription before OAuth using the transport-construction ceremony ID, without inspecting a CCDP value. A successful MessagePort authentication closes it without creating an `RTCPeerConnection`, SDP, or ICE candidate. A local callback timeout commits RTC once; late MessagePort authentication and duplicate signaling are inert. |
| LIBID-BROWSER-027 | In RTC fallback, the final top-level prover is the offerer and the application is the answerer. They use trickle ICE with STUN and one ordered reliable `RTCDataChannel`; connection does not depend on mDNS, local-network permission, or ICE gathering completion. Signaling exact-checks the allowed application origin and configured ceremony-server origin; the one-use ceremony ID correlates but does not authenticate the roles, and the signaled fingerprints bind the DTLS channel. Signaling carries no CCDP message or ceremony payload. |
| LIBID-BROWSER-028 | The RTC callback queues exactly one bounded `CallbackDeliverParams` on a local `MessagePort`, hands the peer port to the worker as `rtc-bootstrap`, and navigates. The final prover claims and consumes it, opens RTC, and forwards that unchanged message first. The callback and worker create no peer and cannot inspect DTLS-protected CCDP traffic. |
| LIBID-BROWSER-029 | One concrete `CCDPTransport` carries its caller-owned `Message` type, selects from native `MessagePort` and `RTCDataChannel` resources, and contains no hardcoded CCDP message type, route, or semantic phase. Each endpoint registers a decoder and typed handler per permitted inbound discriminator; transport calls the selected decoder exactly once and delivers only its result. Both carriers preserve ordered nonduplicated logical values directly between browser endpoints; no server relays or stores them or terminates their channel. Malformed input, RTC framing, signaling/ICE failure, carrier loss, or remote-context destruction returns no result and never reconnects, switches carrier, or resends a proof request. |
| LIBID-BROWSER-030 | Every CCDP message round-trips through native MessagePort structured clone and the WebRTC JSON/binary codec into the same registered decoder. MessagePort decoding returns the received object unchanged; WebRTC restores every `Uint8Array` and preserves the same logical value. An RTC value outside its logical domain, malformed or reserved byte tag, invalid UTF-8 or JSON, invalid frame order or length, oversize, or buffer overflow reaches no transport handler; an unknown or structurally invalid CCDP record reaches no protocol handler. |

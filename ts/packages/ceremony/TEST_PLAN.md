# `@libid/ceremony` test plan

This plan tests the `@libid/ceremony` implementation architecture in
[ARCHITECTURE.md](ARCHITECTURE.md).
Normative proof and platform behavior come from the
[common ceremony rules](../../../specs/ceremony-common.md) and
[identity-platform ceremonies](../../../specs/platform-ceremonies.md).

Every row is one stable requirement and may require multiple positive and
negative cases. Tests **must cite its ID**. Existing IDs retain their names;
never renumber or reuse one.

A row belongs here only when it exercises `@libid/ceremony`, an emitted browser
asset, or a server/browser contract declared by the package. Consumer-owned Job
transitions, wallet preparation or confirmation, transaction submission, and
post-ceremony recovery belong to their respective consumer test plans.

## Package and asset isolation

| ID | Assertion |
|---|---|
| LIBID-MOD-002 | The `@libid/ceremony/prover` entrypoint emits one dual-context `libid-ceremony-prover.js`: its Window branch owns fixed native-DOM proving/progress UI and its ServiceWorker branch owns only fixed-asset warmup. Its bundle contains no final-proof verifier, redirect, React/UI framework, wallet key, connector, Registry submission, or wallet-policy code. |
| LIBID-MOD-003 | The `@libid/ceremony/popup` entrypoint emits `libid-ceremony-popup.js` with fixed native-DOM ceremony/progress UI; its bundle contains no proving implementation, React/UI framework, wallet key, connector, Registry submission, or wallet-policy code. |
| LIBID-MOD-011 | `@libid/ceremony/protocol` owns the one exact `ServerConfig` schema and validator, including a nonempty duplicate-free `verifierVersions` list for each configured platform. The ceremony client and popup each fetch and validate it with no configuration module or application-supplied integration fields. |
| LIBID-MOD-012 | The popup loads `/oauth/prove` before its opener handshake and the prover artifact owns both warmup and proof execution. No client warmup API, separate warmup module, artifact, route, message, mode, or separately versioned package exists. |
| LIBID-MOD-013 | `@libid/ceremony` contains no server handler or secret. `platforms/github` implements only the normative bounded `TokenRequest` and `TokenResponse` codecs and validation for the server-provided token endpoint; protocol, client, popup, prover, worker, platform, and browser exports build with server implementation unavailable. |
| LIBID-MOD-014 | The ceremony client's public operation input contains exactly the composition-selected Chain Profile's canonical identifier string, enabled `platformId`, namespaced operation-domain string, and bounded opaque `transactionData`. It rejects unknown fields and caller-supplied derived hash fields, verifier version, authorization nonce, authorization digest, OAuth state, OAuth client, redirect, proof-domain, Registry, or finalizer values. |
| LIBID-MOD-015 | For every `CeremonyClient.new`, the client hashes the UTF-8 canonical chain identifier and operation domain into their exact 32-byte normative fields, selects its newest locally preferred `PlatformVerifierVersion` also advertised by that platform's `ServerConfig`, draws a fresh cryptographically secure 32-byte authorization nonce, and reproduces the normative authorization digest. It freezes every derived value before provider navigation; mutating any input or derived field fails exact-match validation before browser launch or proving. |
| LIBID-MOD-016 | `@libid/ceremony/protocol` builds as a pure leaf with browser popup, prover, platform OAuth/proving implementation, network, storage, Job, wallet, connector, and UI code unavailable. Client, popup, and prover consume the same codecs and reject directionally invalid messages; the client uses the protocol-owned final-proof verifier. `CeremonyEvent` is exactly a progress event or validated OAuth-result event. Progress admits only authorization, oauth-validation, prover-activation, and proof-generation and carries an optional bounded opaque platform step with started, completed, or failed status; OAuth result admits only accepted or denied. Both use a ceremony-stamped numeric timestamp and contain no counter, error code, identifier, or broader Job stage. |
| LIBID-MOD-017 | One application-scoped `CeremonyClient` fetches and validates `ServerConfig`, excludes configured platforms missing from its closed implementation or sharing no verifier version with it, and creates independent stateful ceremonies through `new(ceremonyId, input)`. Calling `new` for an excluded platform fails before launch. No package-global singleton, generic Host, mutable popup setter, Job-store dependency, Registry callback, or composition plugin exists. |
| LIBID-MOD-018 | A created `Ceremony` exposes only its public authorization, fixed launch descriptor, event subscription, `proveUserIdentity`, and best-effort `cancel`; its internal ceremony ID is not repeated through the instance API or Identity. `proveUserIdentity()` with no options synchronously opens the empty `/oauth/redirect` `popupHref` before its first await; `proveUserIdentity({ popup })` reuses a non-null handle, while explicit null records that native anchor navigation to the provider `href` is already proceeding and opens nothing else. Both paths use the same target, and the caller prevents native navigation only after opening `popupHref` returns a usable handle. |
| LIBID-MOD-019 | `@libid/ceremony/client` exports one immutable `supportedPlatforms` containing every key of its closed platform implementation table exactly once. Its static `PlatformId` accepts exactly those values; mutation, duplication, omission, an unknown value, and dynamic platform registration are unavailable. |
| LIBID-MOD-020 | Each `CeremonyClient.enabledPlatforms` is an immutable intersection of `supportedPlatforms` and validated `ServerConfig` keys having at least one mutually supported verifier version, in catalog order. It exposes no OAuth client, verifier version, server configuration, or display metadata, and mutating a returned value cannot change ceremony admission. |
| LIBID-ASSET-002 | After URL clearing, redirect and prover bootstraps dynamically append their exact root `modulepreload` with immutable URL, SRI, and anonymous CORS, await success, and import that identical URL from the module map; changing bytes, URL, integrity, origin, media type, or COEP compatibility fails before execution. |
| LIBID-ASSET-003 | The root module bundles ordinary transitive JavaScript; every remaining worker/WASM URL is immutable and no ceremony, opener, query, or server response may supply one. |
| LIBID-ASSET-004 | A `PlatformVerifierVersion` owns one platform's exact proof construction, public-input validation, and verifier compatibility; the ceremony client selects its newest locally preferred version advertised by the platform configuration and freezes it in `CeremonyAuthorization` and the private `CeremonyRequest`. |
| LIBID-ASSET-005 | Changing one platform verifier version does not version another platform or `PopupProtocolVersion`; application input cannot select it, and `ServerConfig` only constrains selection to its advertised set. |
| LIBID-ASSET-006 | Every live redirect/prover page retains the modules it loaded; the popup dispatches the ceremony's exact verifier version and never resolves `latest` during a ceremony. |
| LIBID-ASSET-007 | Every live `Ceremony` and private `CeremonyRequest` pin the exact public `PlatformVerifierVersion`; the popup retains every version it still accepts during its compatibility window and never resolves `latest` after creation. An unsupported version cannot be consumed and requires a fresh ceremony. |
| LIBID-ASSET-009 | A real external worker graph passes with the outer blob bridge and every nested bb.js/TLSNotary worker; direct cross-origin `new Worker(url)` is rejected and no unisolated fallback runs. |
| LIBID-ASSET-010 | Rotate `ServerConfig` while a ceremony is live: its private request retains the exact client ID, redirect URI, and verifier version selected at creation; the redirect accepts them only while its independently fetched configuration still supports them. An unsupported or substituted setup clears returned credentials, performs no exchange, proving, proof delivery, or state transition, and renders the fixed retry result. New ceremonies use the client's newest locally preferred advertised verifier version. |

## Ceremony client and popup messages

| ID | Assertion |
|---|---|
| LIBID-OAUTH-001 | Frontend and redirect independently fetch and exact-validate server-owned `ServerConfig`; unknown fields, missing or wrongly typed values, noncanonical, empty, or out-of-range client, redirect, origin, platform, or verifier-version values, and duplicate verifier-version entries fail before navigation or live ceremony routing. Duplicate origins use the `Set` semantics of KIT-004. An unknown but valid verifier version is ignored when another advertised version is locally supported; a platform with no common version is unavailable. |
| LIBID-OAUTH-002 | The frontend configuration request requires an exact browser `Origin` and matching noncredentialed CORS response. After starting or joining fixed-asset warmup, the cleared popup sends only a versioned `PopupHello` discovery message to `window.opener`; the app accepts it only from the configured redirect origin and answers with the same `PopupHello` directly to that source/origin. The popup accepts the answer only from its exact opener and a server-allowed browser-stamped app origin. No redirect-request `Origin`/`Referer`, client origin field, server session, or server action record authenticates the channel. |
| LIBID-OAUTH-003 | The ceremony caller may select a Chain Profile per ceremony and supply only its canonical chain ID, platform ID, operation domain, and transaction data. It cannot substitute server, client, redirect, verifier version, authorization nonce/digest, notary, asset URL, or proof destination. |
| LIBID-OAUTH-005 | Redirect bootstrap bounds and copies both URL components and clears query/fragment before parsing, storage, rendering, errors, module fetch, or any connection, including malformed and oversized input. |
| LIBID-OAUTH-006 | The ceremony popup requires the original exact opener/source/origin and sends `PopupOAuthResult` with the captured OAuth state after receiving a provider OAuth result. The provider response remains inside the popup. The application-scoped client responds exactly once with `PopupCeremonyStart` after atomically claiming one matching live Ceremony from its in-memory map, or with parameterless `PopupAbort` when none exists. It performs no IndexedDB lookup and never exposes the state to the composition. Openerless, absent, stale, replayed, post-reload, wrong-origin, and wrong-version results restart OAuth. |
| LIBID-OAUTH-007 | The selected platform ceremony rejects wrong transport, mixed/empty transport, malformed encoding, duplicate authoritative fields, mixed success/error, wrong state/client/redirect/platform, and a Google ID Token at or after its signed `exp` before proving. Mutable X/GitHub proof lifetimes are tested at the Platform Verifier, not enforced by the browser. |
| LIBID-OAUTH-009 | DIP binds the exact parent/child `WindowProxy` and browser-stamped origin; popup mode clears its ceremony-ID fragment and uses the same-origin `BroadcastChannel` derived from that ID without relying on `window.opener`. Each is a one-shot ceremony channel, so only Start's request and the delivered proof carry the ceremony ID. Neither path claims a separate prover-authentication boundary. |
| LIBID-OAUTH-010 | The prover sends `ProverHello` only after confirming cross-origin isolation and `SharedArrayBuffer`; no credential crosses before Hello and no user-agent branch selects a weaker mode. Version-specific worker initialization follows Start, and failure clears inputs and sends `ProverAbort` rather than selecting fewer workers or an unisolated mode. |
| LIBID-OAUTH-011 | Before proof acceptance, the durable Job contains no `CeremonyAuthorization`. A restart creates a fresh nonce, digest, and OAuth state. Provider credentials and private witnesses remain memory-only and never enter IndexedDB, local/session storage, a later URL, app messages, progress, logs, traces, analytics, or errors. Until X/GitHub token exchange completes, the raw authorization nonce also remains memory-only and crosses only the authenticated application/popup/prover messages; afterward it may be stored only as part of the accepted `OAuthProof`. |
| LIBID-OAUTH-012 | The isolated prover delivers only the bounded normative `OAuthProof` through `ProverDeliverProof` and imports no final-proof verifier. The record includes the proof, required attestations, and authorization fields. After live delivery, app-side `@libid/ceremony/protocol` checks that exact `OAuthProof` and derives Identity only from locally verified proof inputs and authenticated attestation bytes. Callers cannot supply or override Identity fields. |
| LIBID-OAUTH-013 | The ceremony popup accepts `ProverDeliverProof` only in the started prover state, bounds its `oauthProof`, exact-matches its `ceremonyId` to `PopupCeremonyStart`, and sends those same fields through `PopupDeliverProof` only over the live authenticated app channel. Interruption before `proveUserIdentity()` verifies and returns an accepted `IdentityResult` requires fresh OAuth. |
| LIBID-OAUTH-014 | The live Ceremony accepts delivery only from its authenticated source/origin and exact internal ID, checks the `OAuthProof`, derives Identity with that same `OAuthProof` and authenticated client identifier, and resolves `proveUserIdentity()` with `{ status: 'accepted', identity }`. That status means provider approval and local checks succeeded, not Consumer acceptance. Wrong origin/ID, cancellation, or replay returns no accepted result. Only Consumer acceptance makes the preview authoritative. |
| LIBID-OAUTH-015 | The GitHub token endpoint is synchronous and stateless, with no action, proof job, polling, result, progress, cancellation, or retry record; launch exposes no browser TLS bridge route. |
| LIBID-OAUTH-016 | `OAuth` freezes only the `ServerConfig`-selected client and redirect plus the independently random app-origin OAuth state. |
| LIBID-OAUTH-017 | An answering `PopupHello` from the actual opener whose browser-set origin is not an exact member of `ServerConfig.allowedAppOrigins` clears the in-memory capture, sets `window.opener = null`, performs no live-state routing, storage, credential-bearing network request, token exchange, proving, or credential-bearing message, and renders only the fixed close/restart result; messages from unrelated `WindowProxy` sources are ignored. |
| LIBID-OAUTH-018 | A valid state-bound provider denial sends one `PopupNotifyEvent` containing `{ type: 'oauth-result', status: 'denied' }`, then `PopupDenied`, resolves `proveUserIdentity()` with `{ status: 'denied' }`, and starts no exchange or proving. Popup closure, malformed return, and configuration mismatch emit no OAuth-result event. A failure after accepted OAuth emits the accepted event followed by popup-to-application `PopupAbort` and rejects rather than resolving as denial. |
| LIBID-OAUTH-019 | Malformed, unknown-state, unsupported-verifier-version, and internal-validation failures return no Identity, send popup-to-application `PopupAbort` when a live Ceremony has been selected, and render the fixed restart result. A valid success atomically claims its live OAuth-state map entry, sends one `PopupNotifyEvent` containing `{ type: 'oauth-result', status: 'accepted' }` before exchange/proving, runs one attempt, and permits no second `PopupCeremonyStart`; cancellation or a racing terminal outcome makes a late result inert. |
| LIBID-OAUTH-020 | Missing or silent opener, or no valid answering `PopupHello`, reaches the same fixed rejection at exactly `REDIRECT_OPENER_TIMEOUT_MS`, clears in-memory redirect bytes, severs any opener, and never hangs or navigates to attacker-controlled content. |
| LIBID-OAUTH-021 | `ProverCeremonyStart` contains exactly the private `CeremonyRequest` and bounded `oauthResult` string. The request already owns its platform, verifier version, ceremony ID, immutable authorization commitment, and OAuth values; those fields select whether the result is a Google ID token or X/GitHub code. No platform-input union, duplicate ID, redirect wrapper, or application-origin field exists. |
| LIBID-OAUTH-022 | Every member of the closed `PopupMessage` union is a named `Popup…` record with a matching `popup-*` discriminator. The bidirectional `PopupHello` exact-validates one fixed `PopupProtocolVersion`; every later member carries no version, retains the authenticated source/origin pair, and exact-validates under that channel version. `PopupOAuthResult` receives exactly one `PopupCeremonyStart` or parameterless `PopupAbort`; application-to-popup Abort may also cancel a started ceremony, while popup-to-application Abort rejects it after technical failure. Direction supplies the meaning; Abort carries no reason and has no response. Mutate protocol version, type, order, source, browser-stamped origin, ceremony ID, or start fields—or add an abort reason, job revision, expected revision, progress counter, or any other unknown field—and require no state change. No client-configured or negotiated version exists. |
| LIBID-OAUTH-023 | Redirect-origin persistence contains no action, credential, token-exchange attestation, transcript, witness, PKCE value, prover input, generated `OAuthProof`, verified result, or proof-delivery record. |
| LIBID-OAUTH-024 | Missing, canceled, wrong-ID, wrong-origin, unsupported-verifier-version, and replayed live deliveries fail without returning Identity or reviving a Ceremony. |
| LIBID-OAUTH-025 | Destroy the application-side `Ceremony`, ceremony popup, DIP child, isolated prover popup, or application document after proof generation but before `proveUserIdentity()` resolves. No proof can be recovered; a new ceremony requires fresh OAuth. |
| LIBID-OAUTH-026 | An empty `/oauth/redirect` may only start warmup and the opener handshake; it cannot request or recover a proof. Without a valid live opener it renders the fixed restart result without app or ceremony-state mutation. |
| LIBID-OAUTH-027 | `proveUserIdentity()` either opens synchronously or receives one already-open `WindowProxy`, runs the complete one-shot flow, and resolves with exactly one `IdentityResult`: accepted with verified Identity or denied. Any non-popup-protocol message is unknown on that channel. |
| LIBID-OAUTH-028 | `ProverMessage` contains exactly `ProverHello`, `ProverCeremonyStart`, parameterless bidirectional `ProverAbort`, `ProverNotifyEvent`, and `ProverDeliverProof`, each with the matching `prover-*` discriminator. Unless either side aborts first, the prover sends Hello, the popup sends exactly one Start, and the prover sends zero or more events followed by one proof. Invalid order, replay, post-terminal traffic, unknown fields, duplicate ceremony IDs, abort reasons, and messages outside the one-shot bound channel change no state. Popup and prover are one immutable deployment, so no prover protocol version or negotiation exists. |

## Deployment and server configuration

| ID | Assertion |
|---|---|
| KIT-001 | The deployment exposes stateless `GET /_libid/config`, fixed `GET /oauth/redirect`, fixed `GET /oauth/prove`, and only the confidential token/bridge routes required by enabled platforms; it exposes no OAuth preparation route. |
| KIT-002 | Selecting GitHub requires the integrating server to implement and mount the normative fixed token endpoint and fails when its client secret, notary configuration, route, or immutable assets are absent. The ceremony package contributes only `platforms/github` browser request/response codecs and validation. |
| KIT-003 | Google or X Proxy requires no server ceremony state or confidential exchange route beyond public `ServerConfig` and fixed page/asset hosting; launch exposes no browser-MPC bridge route. |
| KIT-004 | `allowedAppOrigins` accepts a nonempty server-controlled list of canonical origins with no protocol maximum; membership uses `Set` semantics, so permutations and duplicates behave identically, each listed origin can fetch configuration and complete the same redirect flow, and an unlisted origin fails. |
| KIT-005 | Frontend and redirect independently fetch the same exact `ServerConfig`. The frontend request requires its browser `Origin`; the redirect's same-origin fetch supplies no application identity, and redirect code authorizes only browser-stamped `MessageEvent.origin`. The callback bootstrap passes only `OAuthRedirectCapture`. |
| KIT-006 | The redirect URI is canonical HTTPS, has no credentials/query/fragment, and has path exactly `/oauth/redirect`; the explicit loopback profile is the only exception. |
| KIT-007 | `ceremonyId` is a lowercase UUIDv4 string. Authorization nonce and OAuth state are mutually independent 32-byte random values; OAuth state serializes as unpadded base64url. The ID is public correlation and is never used as a nonce or capability. For X and GitHub, the authorization nonce remains private until token exchange completes and supplies the PKCE secret. |
| KIT-008 | Reusing a live ceremony ID, or supplying malformed, padded, derived, wrong-version, wrong-variant, or wrong-case values, fails without weakening state or channel checks. |
| KIT-009 | Redirect/prover HTML and headers are byte-identical across requests; no request field changes CSP, script URL, origin, platform, or mode. |
| KIT-010 | The CSP-hashed inline redirect bootstrap performs only capture, clearing, integrity-pinned modulepreload/import, and exported-entrypoint invocation; it never interprets identity-platform fields or accesses storage/network before clearing. |
| KIT-011 | X/GitHub redirect queries are absent from server/proxy logs, traces, analytics, and error reports. |
| KIT-012 | The redirect HTTP response and `libid-ceremony-popup.js` perform no token exchange or server mutation. After URL clearing, opener/request authentication, one-use state claim, and transfer of the closed GitHub provider-return variant, only the isolated GitHub prover calls the fixed stateless token route. |
| KIT-013 | The normative `/oauth/github/token` route requires `Origin` equal to the redirect/prover origin, rejects cross-origin preflight and redirects, accepts only POST with no query and exact JSON `TokenRequest`, uses server-configured client/secret/redirect/notary settings, and returns only status 200 with exact JSON `TokenResponse`. GET, another method, another media type, or malformed JSON fails before token exchange. The server implements the endpoint; `platforms/github` owns only its browser codecs and validation. |
| KIT-014 | Token-exchange mutation, timeout, restart, duplicate request, and response loss retain no server state. The valid bounded response remains browser memory-only; loss before `proveUserIdentity()` returns requires fresh OAuth. |
| KIT-015 | No API input, request, user control, retry, or browser probe selects browser-MPC transport; every launch X and GitHub ceremony uses the deployed Proxy transport. The notary itself opens the fixed platform socket, and no application, request, redirect configuration, or prover message supplies its proxy, socket, destination, or routing. |
| KIT-016 | The deployment serves `libid-ceremony-prover.js` from the redirect origin so the same URL can run as a module service worker; externally distributed nested workers/WASM still pass SRI/CORS and isolation loading. Replacing either source is a full code-supply-chain trust choice. |
| KIT-017 | A mutable root-module URL, missing SRI, broad script/connect source, request-derived CSP, or asset redirect is rejected as a production deployment. |
| KIT-018 | Redirect/prover pages are served from a dedicated cookie-free origin with no unrelated same-origin API in the reference deployment. |
| KIT-020 | The ceremony requires no canonical orchestration service, preparation endpoint, continuation route, status server, server action ledger, or proof-recovery service. Its server surface is public `ServerConfig`, fixed browser documents/assets, and any fixed confidential platform routes. |

## Browser isolation and response policy

| ID | Assertion |
|---|---|
| CSP-001 | `/oauth/redirect` is top-level, non-isolated, no-store/no-referrer, non-frameable, and preserves the application opener. |
| CSP-002 | The single warmup/proving `/oauth/prove` response sends DIP isolate-and-require-corp, COOP same-origin, COEP require-corp, no-store/no-referrer, and same-origin-only framing for DIP. COOP severs only the top-level popup opener and does not create a separate service-worker registration or cache. |
| CSP-003 | Both documents use `default-src 'none'`, `object-src 'none'`, `base-uri 'none'`, `form-action 'none'`, no broad `https:`, and no JavaScript `'unsafe-inline'`/`'unsafe-eval'`; one exact CSP hash permits only the deployment-generated inline bootstrap. |
| CSP-004 | Route headers and asset URLs are fixed deployment values; hostile query/fragment/origin strings cannot inject or expand CSP. |
| CSP-005 | The redirect bootstrap records query/fragment transport, clears both URL components, and makes no storage/network/module action while raw redirect bytes remain in the URL. |
| CSP-006 | The DIP prover accepts an empty query and fragment and receives all bootstrap data only over its exact `WindowProxy` channel. The popup prover accepts only its ceremony ID in the fragment and clears it before appending `modulepreload` or opening storage, `BroadcastChannel`, worker, or network. |
| CSP-007 | In Chromium, Firefox, WebKit, Pixel emulation, and iPhone WebKit emulation, each bootstrap appends the exact immutable root-module URL with SRI and `crossorigin=anonymous`, waits for `load`, imports the identical URL once, and reaches the exported entrypoint; preload failure, a second root fetch, an ambient-global handoff, wrong MIME, missing `nosniff`, CORS failure, or COEP incompatibility fails. |
| CSP-008 | Direct cross-origin Worker construction fails; the allowed local blob bootstrap imports only the immutable configured worker module. |
| CSP-009 | Spawner workers permit only required `blob:`/worker/connect sources; leaf workers use `worker-src 'none'`; every worker has its own CSP. |
| CSP-010 | Real nested bb.js/TLSNotary workers and WASM pass from the configured external distributor through the recursive bridge; no same-origin mirror of that nested graph is required. A deployment which chooses one qualifies it as a separate asset profile. |
| CSP-011 | X and GitHub launch prover profiles permit only their pinned Proxy-notary origins, GitHub additionally permits only its same-origin token route, Google permits only JWKS, and no launch prover permits a TLS bridge origin. |
| CSP-013 | The redirect creates one same-origin `/oauth/prove` iframe during the opener handshake. If it qualifies under DIP, it sends `ProverHello` and becomes the source-bound prover; otherwise it remains warmup-only, receives no credential, and a user-opened top-level `/oauth/prove` popup performs proving. The non-isolated redirect never navigates itself into isolation. |
| CSP-014 | The isolated popup path has zero reliance on opener. Its cleared fragment contains only the ceremony ID used to derive its one-shot `BroadcastChannel`; Start's request and the delivered proof exact-match that ID while Hello, Abort, and advisory events do not repeat it. No role, credential, or proof bytes enter its URL, and the protocol claims no authority beyond the trusted same-origin deployment. |
| CSP-015 | Every content-addressed prover, worker, and WASM response uses exact MIME, `nosniff`, CORS/CORP compatible with isolation, and `Cache-Control: public, max-age=31536000, immutable`; retained entries for the exact release serve the warm path. |
| CSP-016 | Every launch profile runs its exact multithreaded real-prover configuration in both qualified DIP and popup placements. Missing isolation or shared memory prevents `ProverHello` and therefore credential transfer; an active top-level prover sends `ProverAbort`, while an unqualified iframe remains warmup-only. Missing required workers or nested-worker startup after Start also sends Abort, clears inputs, and never selects a single-threaded or unisolated fallback. |
| CSP-017 | Every scripted ceremony opens empty `/oauth/redirect`, which loads `/oauth/prove` and starts or joins warmup before sending `PopupHello`; a bounded unavailable/registration-failure path also reaches Hello without waiting for downloads. The application answers with `PopupHello` and immediately navigates the retained popup to the provider. Native-link fallback may begin directly at the provider, and its callback performs the same warmup attempt before Hello. No developer/application switch, URL flag, warmup route, separate artifact, or public warmup message exists. Cold and warm paths complete the request direction of X's first notarized token session within its code deadline. |
| CSP-018 | The exact same-origin `libid-ceremony-prover.js` URL loaded by `/oauth/prove` runs as both Window module and module service worker with scope covering `/oauth/`. The worker accepts only the profile's exact immutable asset set, including whole `200`-response CRS URLs, and extends the initiating event through completion. Unknown origins/URLs, redirects outside the set, opaque responses, range/`206` substitutes, mutable aliases, and ceremony-supplied assets fail without entering the cache. |
| CSP-019 | Exercise cold, partially evicted, fully warm, concurrent-ceremony, reload, worker-termination, quota/fetch failure, and unsupported-worker paths in both DIP and COOP-popup placements. The worker owns each fetch from its first byte, fetches only missing entries, and makes normal prover requests join the same in-flight request or read its completed cache entry. OAuth navigation and COOP do not duplicate or partition that work. New documents reconnect instead of awaiting a destroyed document's Promise; completed cache entries need no durable completion marker. Failure follows the identical cold path without weaker isolation, fewer threads, or correctness dependence on warmup. |

## Browser lifecycle and progress

| ID | Assertion |
|---|---|
| LIBID-BROWSER-001 | `Ceremony.launch` supplies a real anchor and synchronous `window.open` with the same exact non-reserved `libid-${ceremonyId}` target and neither `noopener` nor `noreferrer`; a null script result leaves native navigation untouched and the resulting redirect has the original app opener. |
| LIBID-BROWSER-002 | Strictly isolated application pages fail before OAuth navigation. A removed opener, framing, synthetic/delayed launch, or opaque sandbox origin fails before callback credentials are used or proving starts. |
| LIBID-BROWSER-003 | The ceremony reuses one auxiliary context through identity-platform authorization and redirect/prover execution. The client retains either the non-null scripted-open handle or the exact browser-stamped source learned from the returning child handshake; the redirect accepts only the original authenticated app opener and exact app origin. |
| LIBID-BROWSER-004 | Each target behaviorally selects DIP only when the child is isolated with shared memory and otherwise exposes the isolated-popup fallback; Chromium, Firefox, WebKit, Pixel emulation, and iPhone WebKit emulation pass their selected path without user-agent sniffing. |
| LIBID-BROWSER-005 | The isolated popup is opener-independent; `ProverHello`, Start, advisory events, Abort, and proof delivery use the same-origin channel derived from the exact ceremony ID. Concurrent or stale ceremony IDs do not mix. |
| LIBID-BROWSER-006 | The Ceremony client reports only `authorization`, `oauth-validation`, optional `prover-activation`, and `proof-generation`. Platform modules report only opaque steps and cannot select a common stage; prover-side steps use `ProverNotifyEvent`, while popup-side steps are attached locally. The popup attaches them only to its current `proof-generation` stage, and the client rejects unauthenticated or illegal transitions. No composition-owned preparation, confirmation, submission, or finality stage enters `CeremonyEvent`. |
| LIBID-BROWSER-007 | Google and public-X relay post-isolation progress through prover → redirect → app with no server status relay; missing live relay leaves the prover popup visibly authoritative for UX only. |
| LIBID-BROWSER-008 | One live ceremony popup serializes progress messages. Duplicate, missed, or artificially reordered delivery affects UX only and cannot authorize, cancel, alter, or complete a ceremony. Channel loss provides no ceremony recovery. |
| LIBID-BROWSER-009 | Popup closure is neither success, denial, nor cancellation. A lost live ceremony returns no Identity and requires fresh authorization; downstream consumer state is outside this package. |
| LIBID-BROWSER-010 | In cold and verified-warm profiles, the complete request direction of X's first notarized token session reaches X before the authorization-code deadline. Delaying completion of that request direction past the deadline abandons the ceremony; response receipt, the identity session, proving, and delivery may complete later. |
| LIBID-BROWSER-011 | Real-device tests cover each platform app installed/absent, authentication in app/browser, approval/denial, suspension, ignored close, and same-tab/openerless return. |
| LIBID-BROWSER-012 | Openerless/new-profile return fails as restart without consuming another ceremony or claiming cross-profile recovery. |
| LIBID-BROWSER-013 | Race `ceremony.cancel()` against live Identity delivery in both orders. Cancellation sends authenticated parameterless `PopupAbort`; the ceremony popup sends parameterless `ProverAbort` over either live prover channel, then removes the DIP iframe or closes the prover popup. Prover workers/connections stop and inputs clear without an acknowledgement or platform-specific cancel path. A cancellation which wins returns no Identity, and a late result cannot revive the Ceremony. |
| LIBID-BROWSER-014 | Launch two concurrent ceremonies and require distinct target names and `WindowProxy` values; navigation or fallback for either ceremony cannot replace the other's popup. The prover popup remains outside this target contract. |
| LIBID-BROWSER-015 | After OAuth, fallback proving is never opened automatically. Absence of a qualified iframe `ProverHello` exposes a real **Continue proving** anchor; its user activation opens exactly one isolated popup even where Firefox blocks a delayed scripted open. |
| LIBID-BROWSER-016 | The redirect owns prover placement for its lifetime and no persistent prover runs under the original app. Background the app while the redirect is foregrounded on mobile Chromium and require forward progress in the visible redirect-owned placement despite a capability-equivalent app child being throttleable. |
| LIBID-BROWSER-018 | Closing consent without a returned OAuth error emits no denial or terminal result; a valid state-bound provider denial resolves immediately as `{ status: 'denied' }`. `cancel()` opens no auxiliary context and best-effort stops reachable live work. `libid-ceremony-popup.js` owns no GitHub server request; a remote stateless request may finish but no response is used. |
| LIBID-BROWSER-019 | Real-device qualification separately covers iOS memory pressure, suspension/eviction, browser chrome, popup blocking defaults, native-provider handoff, and background scheduling; Playwright WebKit/iPhone emulation cannot satisfy these gates. |
| LIBID-BROWSER-020 | **Restart authorization** never opens a clean redirect or proof-recovery context; when the live ceremony is gone the caller creates a fresh `Ceremony` and uses its initial real-anchor/synchronous-open launch with fresh OAuth. |
| LIBID-BROWSER-021 | Reload destroys the in-memory OAuth-state map and every live Ceremony. No redirect, progress record, stale popup handle, or prior ceremony ID is presented as resumable or can recover proof delivery. |
| LIBID-BROWSER-022 | Destroy and recreate the application-side `Ceremony`, ceremony popup document, DIP child, and isolated prover popup at each point in the live flow. No advisory progress, stale popup handle, or recreated context resumes OAuth, proving, or proof delivery. |
| LIBID-BROWSER-023 | Exercise featureful and featureless auxiliary launch across touch, pointer, desktop-mode, and tablet-with-trackpad cases. Browser promotion between popup and tab changes presentation only; no user-agent/input heuristic, integrator mode, or observed presentation changes security, storage, recovery, or prover placement. |
